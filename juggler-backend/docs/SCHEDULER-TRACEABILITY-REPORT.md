# Scheduler Domain — Test-to-Requirement Traceability Report

**Regenerated:** 2026-07-09 (999.1080) — supersedes the 2026-06-15 Hermes report, which cited 5
deleted test files and marked at least 6 covered requirements as MISSING.
**Reconciled against:** juggler `main` `340e7488` · live suite directory `juggler-backend/tests/scheduler/`

## Where traceability lives now (the live sources — use these, not a snapshot)

This report is no longer a hand-maintained requirement×test matrix. The live, maintained sources are:

1. **`juggler/docs/REQUIREMENTS.md`** — the formal R-number register. Every requirement row carries
   its own **Code** and **Tests** columns plus a status (`implemented` bucket alone lists 206 rows,
   incl. the scheduler families R10–R11, R18–R19, R26, R32–R37, R39–R41, R50.x). This is the
   per-requirement traceability authority; it supersedes both this report and
   `MISSING-REQUIREMENTS-AND-TESTS-REPORT.md` / `TRACEABILITY-AUDIT-CURRENT.md`.
2. **`juggler-backend/tests/scheduler/`** — the live suite directory (58 suites at regeneration
   time; scheduler-adjacent suites also live outside it, e.g. `tests/rowToTaskOverdue.test.js`,
   `tests/unit/mappers/overdue-pastdue-recurring.test.js`, `tests/db/migrations/*`).
3. **`juggler/docs/TRACEABILITY-MATRIX.md`** — the generated service-wide requirement→test matrix
   (999.1213). Regenerate with `node scripts/generate-traceability.js` (from `juggler/`); it verifies
   every Tests-column reference in REQUIREMENTS.md against the live filesystem and flags stale refs
   (`--check` exits 1 on rot). Replaces the former superrepo `.planning/coverage/` system, which no
   longer exists.

Anything load-bearing should be checked against those, not against any generated snapshot.

## Corrections to the 2026-06-15 report (verified against the live suite, 2026-07-09)

**Deleted files it cited (none exist in `tests/scheduler/` today — suite restructured):**
`unifiedSchedule.test.js`, `expandRecurring.test.js`, `rollingAnchor.test.js`,
`reconcileSplits.test.js`, `goldenMaster.a002-location.test.js`.

**"MISSING" verdicts that were wrong — covered with real pinning assertions (per the sched-audit A4
suite-by-suite audit, zoe 2026-07-02; file presence re-verified 2026-07-09):**

| Requirement | Covered by (present in live suite) |
|-------------|-------------------------------------|
| R37.1 / R37.2 — earliest-start hard bound / `>` deadline validation | `earliestStart.test.js` |
| R40.1–R40.3 — flexWhen flag / fallback retry / `_flexWhenRelaxed` | `flexibleEarliestStartRelax.test.js` |
| R19.7 — `partial_split` flag | `split-containment-edges.test.js` |
| R35.6 — `recurring_split_overflow` flag | `split-containment-edges.test.js` |
| R10.3 — cyclic dependency detection | `dependencies.test.js` |
| R11.10 / R39.5 — weather fail-closed | `weatherFailOpen.test.js` (code confirmed fail-closed — SCHEDULER-SPEC.md `[PLACE-WEATHER]`; SCHEDULER-RULES.md §9.3 rewritten fail-closed in this same sweep) |

**Suites added since the old report** (sched-audit + later legs, all present): `sched-audit-l2`,
`sched-audit-da-oneoff`, `sched-audit-dc-rigid`, `sched-audit-reg26-roam`, `dst-transition`,
revived `splitStatusPropagation`, plus post-audit additions such as `allday-overdue` (R50.12, 57
tests), `sched-drop-overdue-column-characterization` (M-5/999.1085), `w2-partition` (juggy4 W2),
`overdue-unscheduled-pinning`, `roamable-recurring-forward-roll`, `rolling-past-due-forward-roll`.

## Known open gaps (tracked, not lost)

- **TS-310 SUB-a/c, TS-311, TS-312:** the corresponding blocks were deleted from
  `splitStatusPropagation.test.js` when it was revived; re-author them ONLY if those sub-features
  get pinned — see the pointer comments left in L4-TEST-REVIEW.md.
- **Per-suite quality findings** (weak/tautological assertions flagged by A4, e.g. in
  `w2-partition.test.js`, `earliestStartWindow.test.js`) are tracked as their own L4 backlog items.
- The A4-TEST-AUDIT.md source file has since been archived out of `.planning/kermit/`; its
  correction claims above were re-verified against the live suite directory at regeneration time.

## Historical note

The original 2026-06-15 matrix (103 requirements, generated by the Hermes agent against a 20-file
suite) is superseded and was removed with this regeneration — its per-row verdicts predate the
suite restructure and are unrecoverable as evidence. Use `juggler/docs/REQUIREMENTS.md` Tests
columns for per-requirement citations. (git history of this file retains the old matrix.)
