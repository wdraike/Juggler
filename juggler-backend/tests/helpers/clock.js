// Test helper for clock-related functionality
// Exports FakeClockAdapter for use in scheduler tests

const FakeClockAdapter = require('../../src/slices/scheduler/adapters/FakeClockAdapter');

module.exports = {
  FakeClockAdapter
};