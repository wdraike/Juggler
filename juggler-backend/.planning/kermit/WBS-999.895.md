# WBS — 999.895 — bugfix — 2026-06-26

## Intent
Mirror the HTTP terminal-requires-schedule guard into the MCP task tools (`set_task_status`,
`update_task`) so MCP clients (ClimbRS) cannot mark a task `done`/`skip`/`cancel` without a scheduled
time. Preserve the rolling-recurring-instance exemption (parity with the HTTP guard).

## Root cause (evidence-first)
- Canonical guard: `src/slices/task/application/commands/UpdateTaskStatus.js:147-160`.
- Gap: `src/mcp/tools/tasks.js` — `set_task_status` (L385-426) writes `status` directly with no guard;
  `update_task` (L238-383) applies a `status` field with no guard.

## Work Items
| ID | Task | Mode | Scope | Inputs | Depends on | Acceptance criteria | Agents | Wave |
|----|------|------|-------|--------|-----------|---------------------|--------|------|
| W1 | RED test proving MCP `set_task_status` + `update_task` skip the terminal-schedule guard | bugfix | juggler-backend | SPEC R1-R5; HTTP guard; tests/helpers/mcp.js | — | Test RED on current code, GREEN after fix; covers block, scheduled-OK, non-terminal-OK, rolling-exempt, update_task | telly | 1 |
| W2 | Mirror the HTTP guard into `set_task_status` and `update_task` (shared module-level helper) | bugfix | juggler-backend | W1 RED test; SPEC | W1 | Guard blocks terminal status on unscheduled non-rolling task in both tools; rolling instances + scheduled tasks + non-terminal pass | bert | 2 |

## Dependency Graph
W2 ← W1 (telly writes RED, bert fixes to GREEN — irreducibly serial bugfix chain).

## Dependency Determination Log
| Dep | Type | Source |
| W2←W1 | build-order | derived — TDD: failing test precedes fix (one cohesive guard, one file, one quality pass → not split) |

## Waves
Wave 1: W1 (telly RED)
Wave 2: W2 (bert fix → telly GREEN → ernie/zoe review → gate)

## Routing decision
- Classifier (`.planning/kermit/999.895/SIZE-CLASSIFY.json`): full lane, non-risky, 30 lines, depth quick.
- Kermit decision: **lane=full, depth=standard** — floored UP from `quick` because this is a task-state-
  machine guard with a rolling-instance exemption edge case (more review, not less).
- Snuffy: **skipped** — non-risky surface, full lane already adopted (no down-scoping for Snuffy to
  challenge) and depth floored UP (no under-review risk). Skip reason recorded here.

## Scooter
Not separately consulted — bugfix with a single canonical in-repo guard already read end-to-end
(`UpdateTaskStatus.js`); the mirror is mechanical parity, no settled-decision relitigation. Prior rule
context: project memory `juggler-844-terminal-guard-vs-undo-conflict` (terminal one-way guard contested)
concerns the SEPARATE one-way/undo guard, NOT this schedule-required guard — out of scope.

## Status: DONE
- Mode classified (bugfix), entry gate met (repro + root cause located), WBS right-sized (1 cohesive
  guard → 2 serial TDD items, not split), traceability seeded, routing recorded.
_Signed: Kermit — 2026-06-26_
