# WBS — 999.894 — chore (docs-only) — 2026-06-26

## Intent
Document the fixed-XOR-recurring invariant (a task is EITHER `placement_mode='fixed'` OR recurring,
never both) — established/enforced in leg 999.867 (commit `60a9e81`) — in both
`juggler-backend/docs/architecture/TASK-PROPERTIES.md` and `.../TASK-STATE-MATRIX.md`.
Accuracy over completeness: state the invariant exactly as enforced, cite `taskValidation.js:98`.

## Work Items
| ID | Task | Mode | Scope | Inputs required | Depends on | Acceptance criteria | Agents | Wave |
|----|------|------|-------|-----------------|-----------|---------------------|--------|------|
| W1 | Document XOR invariant in both target docs per SPEC.md authoring brief | chore | juggler | SPEC.md brief + 999.867 code citations | — | (1) Both docs state the invariant exactly: illegal state = `placement_mode==='fixed'` AND `recurring` truthy. (2) Both cite `taskValidation.js:98` (`isFixedRecurringConflict`). (3) `invalid_combination` rejection outcome documented. (4) TASK-PROPERTIES.md line-34 "any mode can be recurring" note corrected to exclude `fixed`. (5) TASK-STATE-MATRIX.md field-visibility Recurrence/Fixed cell caveated (not silently flipped). (6) Docs only — no code touched. | abby → prairie | 1 |

## Dependency Graph
Single item; no deps.

## Dependency Determination Log
| Dep | Type | Source |
|-----|------|--------|
| — | — | single docs item, no internal deps |

## Waves
Wave 1: W1
