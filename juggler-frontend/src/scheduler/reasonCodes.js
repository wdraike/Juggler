/**
 * Reason codes — re-exports from shared/scheduler/reasonCodes.js
 */

const shared = require('juggler-shared/scheduler/reasonCodes');

export const { REASON_CODES, REASON_LABELS, labelFor } = shared;
