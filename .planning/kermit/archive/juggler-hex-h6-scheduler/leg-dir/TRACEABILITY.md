# Traceability — juggler-hex-h6-scheduler — refactor

Rows = behaviors to preserve (DESIGN §6 invariants + count behavior_contract). Before-test (W0
golden-master, current code) → after-test (same suite, extracted slice) → identical?

| ID | Description (behavior preserved) | Design element | Code (file:sym) | Test(s) | Status |
|----|---------------------------------|----------------|-----------------|---------|--------|
| S1 | Schedule most-constrained → least-constrained (slack asc, pri asc, dur desc, id) | ConstraintSolver.solve ordering | _W1: slices/scheduler/domain/ConstraintSolver_ | goldenMaster.h6.test.js: "S1 — most-constrained → least-constrained ordering snapshot" (3 tests, FROZEN LITERAL) — **PASS**; **W1 domain unit**: solvers.test.js "ConstraintSolver.compareItems — S1" (3 tests: slack asc, null slack, tie-break) + value-objects-entities.test.js "Priority VO" (6 tests: rank/normalize parity) — **PASS** | **Extracted (W1 2026-06-12)** — ConstraintSolver now owns S1; golden-master + domain units both GREEN; MUT-D → 2 RED ✓ |
| S2 | Severity: fixed > overdue > deadline > free; deadlines trump priority | severity comparator | _W1: ConstraintSolver_ | goldenMaster.h6.test.js: "S2 — severity comparator" (4 tests) — **PASS**; **W1 domain unit**: solvers.test.js "ConstraintSolver.severityRank — S2" (1 test) + value-objects-entities.test.js "Constraint entity — S2 severity precedence" (5 tests) + INTEGRATED STEP 1 (1 test) — **PASS** | **Extracted (W1 2026-06-12)** — ConstraintSolver.severityRank + Constraint.severity() own S2; all GREEN |
| S3 | Recurring instances placed same-day only; never roll to adjacent day | same-day placement rule | _W1: ConstraintSolver / placement_ | goldenMaster.h6.test.js: "S3 full-day" (2 tests) + "S3 basic" (2 tests) — **PASS**; **W1 domain unit**: solvers.test.js "recurringCycleDays maps recurrence types" (1 test) + "parseDayReq builds allowed-DOW sets" (1 test) — **PASS** | **Extracted (W1 2026-06-12)** — recurringCycleDays + parseDayReq moved to ConstraintSolver; day-lock logic in unifiedScheduleV2 (thin delegator); MUT-A → 2 RED ✓ |
| S4 | Scheduler triggered by mutation/MCP only; never self-triggers | RunScheduleCommand excludes scheduleQueue | _W4: RunScheduleCommand_ | goldenMaster.h6.test.js: "S4 — static require-graph" (2 tests) — **PASS** | Pinned (W0). W2/W3: update if scheduleQueue.js is renamed or re-exported |
| S5 | **Delta-write (write count = changed tasks) — BEHAVIORAL CHANGE (user 2026-06-12)**, current code writes-all | ScheduleRepositoryPort.writeChanged(delta) | _W2: KnexScheduleRepository_ | goldenMaster.h6.test.js: "S5" (2 × real it()) — calls runScheduleAndPersist, asserts only changed tasks' updated_at changes. it.failing REMOVED. PASS because delta-write is live (runSchedule.js:1404 skip-condition + KnexScheduleRepository.writeChanged). Confirmed: "executing 0 DB updates" on run-2. scheduleAdapters.contract.test.js: "S5 InMemory: only delta rows applied" + "S5 Knex: no now-builder" (2 tests) — **PASS**. | **GREEN (W2 verified 2026-06-12)** — delta-write live; S5 integration tests pass for the right reason; adapter contract 14/14 GREEN |
| S6 | No cascading scheduler calls within a run | RunScheduleCommand returns, no re-enqueue | _W4: RunScheduleCommand_ | goldenMaster.h6.test.js: "S6 — no-cascade: enqueueScheduleRun NOT called" (2 tests) — **PASS** | Pinned (W0). W2/W3: add runtime spy if scheduleQueue path changes |
| S7 | Exact task-type terms (one-off/chain member/recurring instance/split chunk) | PlacementMode VO closed-enum | _W1: domain/vo/PlacementMode_ | H3 golden-master + task.goldenMaster.http.test.js (existing, H3); **W1 domain unit**: value-objects-entities.test.js "PlacementMode is REUSED from the task slice, not duplicated" (1 test) — **PASS** | **Extracted (W1 2026-06-12)** — domain barrel re-exports TaskSlice PlacementMode; no duplication confirmed |
| S8 | Extraction gated by characterization tests (green before+after) | golden-master harness | _W0: tests/characterization/scheduler/_ | goldenMaster.h6.test.js — **45/45 PASS** (H6 W3 isolation fix 2026-06-12); broader scheduler suite 409/410 (1 skip) PASS across 3 runs; --runInBand 45/45 PASS; **Z1 forwarding-contract tests**: schedule-routes.test.js "Z1: forwards correct args" + "Z1: forwards custom x-timezone header" (2 tests, `toHaveBeenCalledWith`) — **PASS 24/24** (zoe WARN Z1 closed 2026-06-12) | **CLEARED + W3 ISOLATION FIX + Z1 FORWARDING CONTRACT (2026-06-12)** — S5/C-IDEM flaky BLOCK resolved; 5× 45/45; Z1: facade drop-arg mutation → 2 RED; revert → 24/24 green |
| P1 | Repos write created_at/updated_at with new Date(), not db.fn.now() | KnexScheduleRepository | _W3: KnexScheduleRepository_ | goldenMaster.h6.test.js: "P1 — migration complete: runSchedule.js has 0 inline fn.now(); writes use new Date() via the repository" (4 tests: 0 inline fn.now in code, KnexScheduleRepository stamps new Date(), RunScheduleCommand.clockNow() instanceof Date, unifiedScheduleV2 pure function) — **PASS** | **GREEN (W3 verified 2026-06-12)** — migration complete; 0 executable fn.now() in runSchedule.js (3 comment-only); KnexScheduleRepository.clock.now() returns new Date(); RunScheduleCommand.clockNow() returns JS Date; flipped from "pin ≥15 fn.now" to "migration complete (0 inline fn.now + repo new Date)"; telly WARN F003: TRACEABILITY row now updated |
| C-IDEM | Back-to-back runs → 0 DB changes (run-2 updated=0) — **enabled BY delta-write** (current write-all is non-idempotent at DB level) | RunScheduleCommand idempotence | _W2/W3_ | goldenMaster.h6.test.js: "C-IDEM" (1 pure-function PASS + 1 real it() integration test) — **PASS (real it())**; it.failing REMOVED; nested describe setup contract preserved (beforeAll/beforeEach outside test body); **W1 domain unit**: solvers.test.js STEP 3 "two consecutive pure-function runs produce identical placements" — **PASS** | **GREEN (W2 verified 2026-06-12)** — delta-write live; DB-level idempotence proven by run-2 updated_at unchanged |
| C-SCORE | ScoreEngine reproduces {total,breakdown} bit-for-bit | ScoreEngine.score | _W1: ScoreEngine_ | goldenMaster.h6.test.js: "C-SCORE — scoreSchedule snapshot" (8 tests) — **PASS**; **W1 domain unit**: solvers.test.js "ScoreEngine.score — byte-identical to legacy scoreSchedule" (5 tests: legacy parity, unplaced=80, deadline=500, frag=30, penalties pinned) + INTEGRATED STEP 3/3b (2 tests) — **PASS** | **Extracted (W1 2026-06-12)** — ScoreEngine.score is now SoT; scoreSchedule.js is a 1-line delegator; byte-identical parity proven |
| C-WX | weatherOk fail-open: no weather data → slot NOT blocked | WeatherProviderPort + core check | _W2 port / W1 core_ | goldenMaster.h6.test.js: "C-WX" (4 tests) — **PASS**; CFG_ACTIVE_WEATHER fixture populates hours 8-11 but leaves hour-12 absent; MUT-B → 2 RED ✓ | **Pinned (W0 fix-loop 2026-06-12)** — weatherOk core logic in unifiedScheduleV2 (thin delegator path unchanged); port extraction in W2 |
| C-COUPLE | rowToTask/taskToRow/buildSourceMap reproduced exactly (task.controller coupling cut) | SchedulerTaskProvider | _W3: SchedulerTaskProvider_ | goldenMaster.h6.test.js: "INTEGRATED multi-phase pipeline" (post-extraction identity) — **PASS** | Seeded (W0); proven in W3 |
