# Traceability — juggler-h5-fixes — bugfix

Bugfix rows = the bug → root cause → fix → regression test. Code/Test/Status filled downstream (telly/bert/Oscar).

| ID | Bug (backlog) | Root cause | Code (file:sym) — fix target | Test(s) — regression | WBS | Status |
|----|---------------|-----------|------------------------------|----------------------|-----|--------|
| B1 | 999.419 — 8s timeout 500s valid slow AI calls | AI budget copied from weather geocode (8s) too tight for 8192-token generation | `GeminiAIAdapter`/`gemini-tracked-call` deadline + env-tunable budget | `tests/unit/aiEnrichment/trackedCallTimeout.test.js` — B1a [RED: resolves when should ETIMEDOUT; env ignored], B1b [GREEN guard] | W1a | open |
| B2 | 999.425 — signalClient bandaid; config/telemetry coupling | trackedGeminiCall forwards one config to SDK + telemetry | `gemini-tracked-call.js` sdkConfig vs telemetryParams split | `tests/unit/aiEnrichment/trackedCallTimeout.test.js` — B2a [RED: abortSignal undefined at SDK boundary], B2b [GREEN guard: modelParams clean] | W1a | open |
| B3 | 999.424 — timeout at wrong altitude | deadline lives in adapter, not the shared call layer | deadline in `trackedGeminiCall`/`raceWithTimeout` | `tests/unit/aiEnrichment/trackedCallTimeout.test.js` — B3a [RED: 500ms ceiling fires — no timeout in trackedGeminiCall], B3b [GREEN guard] | W1a | open |
| B4 | 999.416 — orphaned billing telemetry on abort | finally enqueues after caller got ETIMEDOUT | abort path suppresses `enqueue()` | `tests/unit/aiEnrichment/timeoutAbortConsequences.test.js` — B4-red [RED: enqueue() called 1 time, expected 0] | W1b | open |
| B5 | 999.419 — timed-out call burns daily quota slot | quota logged before the call; timeout still counts the attempt | `ai.controller`/`KnexAIUsageRepository` quota-on-timeout | `tests/unit/aiEnrichment/timeoutAbortConsequences.test.js` — B5-red [GREEN post-fix: checkQuota+no commitQuota=0 rows]; B5-guard [GREEN: checkQuota+commitQuota=1 row]. `tests/api/ai-command.test.js` — AP-72g B5-controller-pin [GREEN: handleCommand+ETIMEDOUT→commitQuota spy=0 calls]; AP-72g B5-warn2 [GREEN: handleCommand+success+commitQuota throws→200 with AI result] | W1b | open |
| B6 | 999.420 — suggest-icon log flood when AI off | _getClient() throws on missing config → route logs error | `GeminiAIAdapter._getClient` not-configured → clean no-op | `tests/unit/aiEnrichment/adapterLifecycle.test.js` — B6-red [RED: mockErrorSpy called 1 time expected 0; logger.error IS called on not-configured path]; B6-guard [GREEN: configured adapter, no error log] | W2a | open |
| B7 | 999.422 — result.text null deref masks error | no null guard; adapter widens non-object returns | adapter/controller result-shape guard | `tests/unit/aiEnrichment/adapterLifecycle.test.js` — B7-red [RED: error is "Cannot read properties of null" not "Unexpected Gemini response structure"]; B7-guard-2 [GREEN: blocked {content:null} variant already structured]; B7-guard [GREEN: valid text response 200] | W2a | open |
| B8 | 999.423 — cached client ignores key rotation | singleton snapshots env + caches client for process life | client invalidation — live-invalidation contract: compare cached key on each _getClient() call; if changed, rebuild | `tests/unit/aiEnrichment/adapterLifecycle.test.js` — B8-red [RED: GoogleGenAI instantiated 1 time not 2 after key rotation; same client returned]; B8-guard [GREEN: same key → cached client reused — 1 instantiation] | W2a | open |
| B9 | 999.421 — db resolve boot→first-request | lazy facade + wrong NODE_ENV string check; getDefaultDb() never called at boot | `facade.init()` boot hook: calls `getDefaultDb()` eagerly; wired into `server.js start()` | `tests/unit/aiEnrichment/adapterLifecycle.test.js` — B9-boot-red [RED: facade.init is not a function; must throw when getDefaultDb() throws at boot]; B9-boot-guard [RED: facade.init is not a function; must resolve when getDefaultDb() resolves]; B9-env-ok [RED: facade.init is not a function; bogus NODE_ENV but db resolves → must NOT throw (proves db-resolution, not NODE_ENV string check)]; B9-boot-assert [GREEN: REFER→bert: wire facade.init() into server.js start()] | W2a | open |
| B10 | 999.417 — prompt-injection / 422 raw-echo | user prompt → Gemini ungated; 422 echoes input | elmo SECURITY-REVIEW on input-trust boundary | per elmo finding | W2b | open |
| B11 | 999.415 — quota TOCTOU | non-atomic count-then-insert | `KnexAIUsageRepository` atomic acquire + migration (atomic mechanism TBD by bert+cookie — FOR UPDATE / unique-constraint / counter table) | `tests/unit/aiEnrichment/quotaTOCTOU.test.js` — B11-race [RED: two concurrent checkQuota+commitQuota at count=49 → finalCount=51, expected ≤50; 51 > 50 on current code]; B11-guard [GREEN: single acquire under limit → exactly 1 row; happy path non-regression] | W3 | open |
