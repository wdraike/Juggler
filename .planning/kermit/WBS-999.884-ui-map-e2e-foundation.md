# WBS — 999.884-ui-map-e2e-foundation — new — 2026-06-26

## Intent
Deliver the FOUNDATION of 999.884 (UI-map + E2E coverage tooling for juggler-frontend) that is safe
to build autonomously, and decompose the milestone-scale remainder. NO live Playwright run against
dev. Business acceptance: a committed UI map (the backbone the ticket asks for first), a TDD'd
coverage-% calculator, a Playwright scaffold with a few coverage-tagged representative specs, and a
written decomposition + run-target decision.

## Scooter / prior knowledge
No router (state-driven views). Brand tokens live in `src/theme/colors.js` (BRAND + getTheme) and
the root `raike-and-sons-brand-guide.md`. Prior incident (project memory `live-uat-agent-cleanup-hazard`):
a background live-Playwright-UAT agent left 281 E2E-TEST- rows in the dev DB and nearly killed dev
servers → binding veto: no live browser run against dev in this leg.

## Work Items
| ID | Task | Mode | Scope | Inputs | Depends on | Acceptance criteria | Agents | Wave |
|----|------|------|-------|--------|-----------|---------------------|--------|------|
| W1 | Author UI map artifact `e2e/ui-map.json` + `e2e/UI-MAP.md` from the Explore-derived inventory (screens/modals/admin/auth + paths, stable ids, source evidence) | new | juggler-frontend | Explore inventory, App.js/AppLayout.jsx, src/theme | — | ui-map.json parses; every entry unique id + evidence; md counts == json counts; ≥26 screen-surfaces + ≥15 paths | build, ernie | 1 |
| W2 | TDD pure calculator `e2e/coverage/ui-coverage.js` + `ui-coverage.test.js` (`computeCoverage(map,coveredIds)` → per-category covered/total + overall %; unmatched ids surfaced) | new | juggler-frontend | W1 schema | W1 | RED→GREEN under `node --test`; covers 0%/partial/100%/unmatched-id; deterministic; no browser | build(TDD), telly, ernie | 2 |
| W3 | Decomposition `e2e/DECOMPOSITION.md` — remaining per-screen authoring split into tracked increments + run-target decision | new | juggler-frontend | W1 | W1 | lists remaining increments mapped to map ids + the run-target options/decision for David | build | 2 |
| W4 | Playwright scaffold `playwright.config.js` + `e2e/specs/*.spec.js` (≥3 representative, each `@covers <id>`) + collector `e2e/coverage/collect-coverage.js` printing the % report; assertion pattern for content/layout/help/branding | new | juggler-frontend | W1, W2 | W1, W2 | config + ≥3 specs each with ≥1 `@covers`; collector prints per-category %+overall+uncovered list using W2; specs authored not run live | build, ernie | 3 |

## Dependency Graph
W2 ← W1 · W3 ← W1 · W4 ← W1, W2

## Dependency Determination Log
| Dep | Type | Source |
|-----|------|--------|
| W2←W1 | data | calculator consumes the map schema (derived) |
| W3←W1 | data | decomposition enumerates remaining map ids (derived) |
| W4←W1 | data | specs tag map ids; collector reads the map (derived) |
| W4←W2 | data | collector calls the calculator; specs use the tag format W2 defines (derived) |

## Waves
Wave 1: W1
Wave 2: W2, W3 (independent, parallel — both only need W1)
Wave 3: W4

## Determination Log (scope)
- Item is effort=LARGE/milestone. Scoped to FOUNDATION per orchestrator instruction; remainder
  decomposed (W3) and tracked, NOT closed.
- No `--trivial`: multi-file new tooling + real logic (calculator) needing TDD + review. Not risky
  surface (no auth/payments/migration/money) but substantial → full lane, depth standard.
- Live-run deliberately excluded (safety veto). `playwright test --list` dry-validate is David-greenlit.
- DEVIATION (mid-leg): discovered a pre-existing tracked `playwright.config.js` (testDir './tests')
  + 5 existing smoke specs under `tests/e2e/`. Build initially overwrote the config and orphaned
  them (regression). Reconciled: config now globs BOTH `tests/e2e/**` + `e2e/specs/**` with the
  original env-driven safe baseURL; collector scans both; the 5 smoke specs were retrofitted with
  `@covers` tags so existing coverage is wired in (W4 scope addition). Added those 5 modified files
  to leg-meta wbs_files. Coverage now 11/42 (26%) with zero unmatched.
