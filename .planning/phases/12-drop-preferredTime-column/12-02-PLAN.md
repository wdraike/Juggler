---
phase: 12-drop-preferredTime-column
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - juggler-backend/src/db/migrations/20260518000300_drop_preferred_time_column.js
autonomous: true
requirements:
  - DROP-PREF-TIME-DB
must_haves:
  truths:
    - "task_masters.preferred_time column no longer exists"
    - "tasks_v view does not include a preferred_time column"
    - "tasks_with_sync_v view does not include a preferred_time column"
    - "preferred_time_mins column is untouched in task_masters and both views"
    - "Migration runs cleanly on knex migrate:latest"
  artifacts:
    - path: "juggler-backend/src/db/migrations/20260518000300_drop_preferred_time_column.js"
      provides: "Knex migration: rebuild both views without preferred_time, then DROP COLUMN"
      contains: "exports.up"
  key_links:
    - from: "migration up()"
      to: "task_masters.preferred_time"
      via: "ALTER TABLE task_masters DROP COLUMN preferred_time"
      pattern: "DROP COLUMN preferred_time"
    - from: "tasks_v CREATE VIEW"
      to: "task_masters"
      via: "SELECT list omits preferred_time column"
      pattern: "preferred_time_mins"
---

<objective>
Drop the preferred_time boolean column from task_masters and rebuild both views so
neither exposes it. Code was cleaned in plan 01 so no active path touches this column.

Purpose: Complete the removal at the schema level. The column is dead weight once
plan 01 ships; leaving it risks confusion and silent NULL reads from any future code.
Output: A single Knex migration that rebuilds tasks_v and tasks_with_sync_v (without
the preferred_time column) then drops task_masters.preferred_time.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@juggler/.planning/phases/12-drop-preferredTime-column/12-01-SUMMARY.md
</context>

<interfaces>
<!-- Current view SQL source: migration 20260518000100_placement_mode_enum_redesign.js -->
<!-- That migration rebuilt both views. Its SELECT lists are the base for the new views here. -->
<!-- Key point: 20260518000100 already omits the boolean preferred_time column from both views. -->
<!-- Only preferred_time_mins appears. So the view bodies in the new migration are a straight -->
<!-- copy of the views from 20260518000100 — no column surgery needed on them.              -->
<!--                                                                                         -->
<!-- The DROP COLUMN must come AFTER the view drop+recreate because MySQL will refuse to     -->
<!-- drop a column that is referenced in a view, even if the view only selects from a UNION  -->
<!-- that does not include that column in that branch.                                        -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 20260518000300_drop_preferred_time_column.js</name>
  <files>juggler-backend/src/db/migrations/20260518000300_drop_preferred_time_column.js</files>

  <read_first>
    - juggler-backend/src/db/migrations/20260518000100_placement_mode_enum_redesign.js —
      read the full CREATE VIEW SQL for both tasks_v and tasks_with_sync_v. Copy those
      exact CREATE VIEW bodies verbatim into this migration's up(). Do not rebuild
      them from an older migration; 20260518000100 is the authoritative current state.
  </read_first>

  <action>
    Create the migration file. The `up` function must execute these steps in order
    inside knex.transaction():

    STEP 1 — Drop both views (MySQL forbids column drops while views exist that reference
    the underlying table, even indirectly):
      DROP VIEW IF EXISTS tasks_with_sync_v
      DROP VIEW IF EXISTS tasks_v

    STEP 2 — Drop the boolean column:
      ALTER TABLE task_masters DROP COLUMN preferred_time
    Use knex.raw for this. Do not drop preferred_time_mins.

    STEP 3 — Recreate both views using the exact SQL copied from migration
    20260518000100_placement_mode_enum_redesign.js. Copy the full CREATE VIEW
    statements verbatim — do not modify any column list or CASE expression.
    Recreate tasks_v first, then tasks_with_sync_v (tasks_with_sync_v selects
    from tasks_v so order matters).

    The `down` function should throw with message:
    'Down migration for drop_preferred_time_column not implemented — the column
     was redundant and its data is unrecoverable without the original boolean writes.'
  </action>

  <verify>
    <automated>cd "/Users/david/Offline Coding/Raike & Sons/juggler/juggler-backend" && npx knex migrate:latest 2>&1 | tail -10</automated>
  </verify>

  <done>
    Migration runs without error.
    `SHOW COLUMNS FROM task_masters LIKE 'preferred_time'` returns 0 rows.
    `SHOW COLUMNS FROM task_masters LIKE 'preferred_time_mins'` returns 1 row.
    `SELECT preferred_time_mins FROM tasks_v LIMIT 1` returns without error.
    `SELECT * FROM tasks_v LIMIT 1` — result set does not include a preferred_time column.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration → MySQL | Raw SQL hardcoded in migration; no user-supplied input — no injection vector |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-DB-01 | Tampering | preferred_time_mins accidentally dropped | mitigate | Migration targets only `DROP COLUMN preferred_time` by exact name; verify step confirms preferred_time_mins still present after migration |
| T-12-DB-02 | Denial of Service | View references preferred_time causing DROP COLUMN failure | mitigate | Steps 1+2+3 ordering: drop views → drop column → recreate views; transaction wraps all three steps |
| T-12-DB-03 | Tampering | View rebuild diverges from current view definition | mitigate | Executor reads 20260518000100 migration and copies CREATE VIEW SQL verbatim |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
After migration runs:
- `SHOW COLUMNS FROM task_masters LIKE 'preferred_time'` → empty result set
- `SHOW COLUMNS FROM task_masters LIKE 'preferred_time_mins'` → 1 row (column still exists)
- `SELECT preferred_time_mins FROM tasks_v LIMIT 1` → no error
- `SELECT preferred_time_mins FROM tasks_with_sync_v LIMIT 1` → no error
- `SHOW CREATE VIEW tasks_v` → CREATE statement does not contain the string 'preferred_time' except in 'preferred_time_mins'
- `cd juggler-backend && npm test 2>&1 | tail -20` → test suite passes
</verification>

<success_criteria>
task_masters.preferred_time column is gone. Both views are rebuilt and queryable.
preferred_time_mins survives in table and both views. All existing tests pass.
</success_criteria>

<output>
Create `.planning/phases/12-drop-preferredTime-column/12-02-SUMMARY.md` when done
</output>
