---
phase: 12-drop-preferredTime-column
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - juggler-backend/src/controllers/task.controller.js
  - juggler-frontend/src/components/tasks/TaskEditForm.jsx
autonomous: true
requirements:
  - DROP-PREF-TIME-CODE
must_haves:
  truths:
    - "rowToTask() no longer maps row.preferred_time to preferredTime on the task object"
    - "taskToRow() no longer writes row.preferred_time when task.preferredTime is set"
    - "The API never sends preferredTime in JSON responses"
    - "The frontend form never includes preferredTime in the fields it submits to the API"
    - "preferred_time_mins (integer) is untouched in both rowToTask and taskToRow"
  artifacts:
    - path: "juggler-backend/src/controllers/task.controller.js"
      provides: "rowToTask / taskToRow with preferred_time boolean removed"
      contains: "preferredTimeMins"
    - path: "juggler-frontend/src/components/tasks/TaskEditForm.jsx"
      provides: "Form that submits only placementMode, not preferredTime"
  key_links:
    - from: "TaskEditForm.jsx buildFields()"
      to: "API PATCH /tasks/:id"
      via: "changed.preferredTime removed; placementMode drives scheduling mode"
      pattern: "placementMode"
    - from: "taskToRow()"
      to: "task_masters.preferred_time"
      via: "write path removed so column is never touched after this plan"
      pattern: "preferred_time_mins"
---

<objective>
Remove all code-level references to the preferred_time boolean — from the backend mapper
functions and from the frontend form — so the column receives no reads or writes before
the DB migration in plan 02 drops it.

Purpose: Code must be clean before the column is dropped. Removing the column while code
still reads it would silently return NULL everywhere; removing the write path first makes
the column safe to drop.
Output: task.controller.js with preferred_time stripped from rowToTask and taskToRow;
TaskEditForm.jsx with preferredTime stripped from buildFields and buildChangedFields.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@juggler/.planning/ROADMAP.md
@juggler/.planning/STATE.md
</context>

<interfaces>
<!-- Key locations extracted from codebase for executor reference -->

From juggler-backend/src/controllers/task.controller.js:

rowToTask() (~line 426):
  preferredTime: row.preferred_time != null ? !!row.preferred_time : null,   // REMOVE this line
  preferredTimeMins: row.preferred_time_mins != null ? row.preferred_time_mins : null,  // KEEP

taskToRow() (~line 502-504):
  if (task.preferredTime !== undefined) {
    row.preferred_time = task.preferredTime === null ? null : (task.preferredTime ? 1 : 0);  // REMOVE entire if-block
  }
  if (task.preferredTimeMins !== undefined) row.preferred_time_mins = task.preferredTimeMins;  // KEEP

TEMPLATE_FIELDS array (~line 159):
  'preferred_time', 'preferred_time_mins', ...  // remove 'preferred_time' from list, keep 'preferred_time_mins'

From juggler-frontend/src/components/tasks/TaskEditForm.jsx:

buildFields() useMemo (~line 414 and ~534):
  preferredTime: t.preferredTime != null ? !!t.preferredTime : null,   // REMOVE from initial snapshot object
  preferredTime: placementMode === 'time_window' ? true : ...           // REMOVE from submitted fields

buildChangedFields() (~line 575-579):
  var snapPref = !!snap.preferredTime;
  if (recurring && hasPreferredTime !== snapPref) changed.preferredTime = all.preferredTime;  // REMOVE both lines

hasPreferredTime state (~line 355-367): KEEP — it drives WhenSection UI display;
  but remove the line: if (task.preferredTime != null) return !!task.preferredTime;
  The fallback tag-count heuristic at lines 363-366 already handles initialization correctly.
  After removal, initialization falls straight through to the tag-count path (correct behavior).

useState initializer for buildFields snapshot (~line 414):
  preferredTime: t.preferredTime != null ? !!t.preferredTime : null,  // REMOVE this key
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Strip preferred_time from backend mapper (rowToTask, taskToRow, TEMPLATE_FIELDS)</name>
  <files>juggler-backend/src/controllers/task.controller.js</files>

  <action>
    Make three targeted edits to task.controller.js — do not touch preferred_time_mins.

    Edit 1 — TEMPLATE_FIELDS (~line 159):
    Remove the string 'preferred_time' from the array. The entry reads
    `'preferred_time', 'preferred_time_mins'`. Change it to just `'preferred_time_mins'`.

    Edit 2 — rowToTask (~line 426):
    Delete the line:
      preferredTime: row.preferred_time != null ? !!row.preferred_time : null,
    The next line `preferredTimeMins: row.preferred_time_mins ...` must stay.

    Edit 3 — taskToRow (~line 502-504):
    Delete the entire if-block:
      if (task.preferredTime !== undefined) {
        row.preferred_time = task.preferredTime === null ? null : (task.preferredTime ? 1 : 0);
      }
    The following line `if (task.preferredTimeMins !== undefined) ...` must stay.

    No other changes. Do not touch any comment that merely mentions preferred_time_mins.
  </action>

  <verify>
    <automated>cd "/Users/david/Offline Coding/Raike & Sons/juggler/juggler-backend" && grep -n "preferred_time[^_]" src/controllers/task.controller.js | grep -v "preferred_time_mins" | grep -v "^\s*//"</automated>
  </verify>

  <done>
    grep returns zero hits for bare `preferred_time` (excluding `preferred_time_mins` and comment lines).
    `preferred_time_mins` still appears in TEMPLATE_FIELDS, rowToTask, and taskToRow.
  </done>
</task>

<task type="auto">
  <name>Task 2: Strip preferredTime from frontend form (buildFields, buildChangedFields)</name>
  <files>juggler-frontend/src/components/tasks/TaskEditForm.jsx</files>

  <action>
    Make four targeted edits to TaskEditForm.jsx — keep hasPreferredTime state and its
    setRecurringHasPreferredTime usage intact (the state still drives WhenSection time-input visibility).

    Edit 1 — hasPreferredTime useState initializer (~line 362):
    Remove only this line from inside the useState callback:
      if (task.preferredTime != null) return !!task.preferredTime;
    The remaining tag-count heuristic (lines ~363-366) becomes the sole initializer.

    Edit 2 — buildFields snapshot object (~line 414):
    Remove the line:
      preferredTime: t.preferredTime != null ? !!t.preferredTime : null,
    The line `preferredTimeMins: t.preferredTimeMins ...` must stay.

    Edit 3 — buildFields return object (~line 534):
    Remove the line:
      preferredTime: placementMode === 'time_window' ? true : (placementMode === 'time_blocks' ? false : undefined),
    The `preferredTimeMins` line that follows must stay.

    Edit 4 — buildChangedFields (~lines 575-579):
    Remove both lines:
      var snapPref = !!snap.preferredTime;
      if (recurring && hasPreferredTime !== snapPref) changed.preferredTime = all.preferredTime;
    The `var snapPlacementMode` line immediately after must stay.

    After edits: hasPreferredTime remains in state, is still set by handleModeChange,
    and is still passed to WhenSection — none of that changes.
  </action>

  <verify>
    <automated>cd "/Users/david/Offline Coding/Raike & Sons/juggler/juggler-frontend" && grep -n "preferredTime[^M]" src/components/tasks/TaskEditForm.jsx | grep -v "hasPreferredTime\|setRecurringHasPreferredTime\|preferredTimeMins\|^\s*//"</automated>
  </verify>

  <done>
    grep returns zero hits for `preferredTime` that are not the hasPreferredTime state variable,
    its setter, or preferredTimeMins. `preferredTimeMins` lines remain. `hasPreferredTime` state
    and its usages in handleModeChange and WhenSection remain.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → DB | taskToRow() was the only write path for preferred_time; removing it closes the write vector |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | preferred_time_mins accidentally removed | mitigate | Verify grep confirms preferred_time_mins still present in all three locations after edit |
| T-12-02 | Denial of Service | Frontend state breakage if hasPreferredTime removed | accept | hasPreferredTime state is explicitly kept; only the API round-trip field is removed |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
After both tasks complete:
- `grep -n "preferred_time[^_]" juggler-backend/src/controllers/task.controller.js` (excluding comments) → 0 hits
- `grep -c "preferred_time_mins" juggler-backend/src/controllers/task.controller.js` → ≥ 3 (TEMPLATE_FIELDS, rowToTask, taskToRow)
- `grep -n "preferredTime[^M]" juggler-frontend/src/components/tasks/TaskEditForm.jsx` (excluding hasPreferredTime/setter/Mins/comments) → 0 hits
- Frontend compiles without error: `cd juggler-frontend && npm run build 2>&1 | tail -5`
</verification>

<success_criteria>
No code path reads or writes the preferred_time boolean column. preferred_time_mins
is untouched. The frontend form submits placementMode as the sole mode signal.
The app compiles and the form opens without runtime errors.
</success_criteria>

<output>
Create `.planning/phases/12-drop-preferredTime-column/12-01-SUMMARY.md` when done
</output>
