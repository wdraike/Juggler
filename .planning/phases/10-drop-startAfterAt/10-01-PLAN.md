---
phase: 10-drop-startAfterAt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - juggler-backend/src/controllers/task.controller.js
  - juggler-backend/src/mcp/tools/tasks.js
autonomous: true
requirements:
  - DROP-STARTAFTERAT
must_haves:
  truths:
    - "No task object returned by rowToTask contains a startAfterAt property"
    - "MCP input schema no longer accepts startAfterAt"
    - "startAfter (date key) and all scheduler usage are untouched"
    - "All backend tests pass"
  artifacts:
    - path: "juggler-backend/src/controllers/task.controller.js"
      provides: "rowToTask without startAfterAt; taskToRow silently ignores startAfterAt input"
      contains: "startAfter: startAfter"
    - path: "juggler-backend/src/mcp/tools/tasks.js"
      provides: "MCP input schema with startAfterAt entry removed"
      contains: "startAfter: z.string().optional()"
  key_links:
    - from: "rowToTask return object"
      to: "startAfter field"
      via: "line 393 in task.controller.js"
      pattern: "startAfter: startAfter"
    - from: "taskToRow"
      to: "start_after_at column"
      via: "startAfter branch only (startAfterAt branch removed)"
      pattern: "task.startAfter !== undefined"
---

<objective>
Remove the redundant `startAfterAt` ISO string field from the task object emitted by `rowToTask()` and from the MCP input schema. The `start_after_at` DB column, the `startAfter` date key, and all scheduler reads are preserved.

Purpose: `startAfterAt` is a duplicate representation of `start_after_at` that is never read by the frontend, scheduler, or any consumer. Removing it reduces task object surface area and eliminates the deprecated MCP alias before it propagates further.
Output: Two edited source files; no DB changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@juggler-backend/src/controllers/task.controller.js
@juggler-backend/src/mcp/tools/tasks.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove startAfterAt from rowToTask output and clean up taskToRow</name>
  <files>juggler-backend/src/controllers/task.controller.js</files>

  <action>
    Two edits in task.controller.js:

    EDIT A — rowToTask output (around line 362–380):
    Delete the `startAfterAtISO` variable declaration and its population block (lines 362–368 as read):
      var startAfterAtISO = null;
      if (row.start_after_at) { ... startAfterAtISO = saStr; }
    Then delete the `startAfterAt: startAfterAtISO,` line from the return object (around line 380).
    The `startAfter: startAfter` line (around line 393) stays untouched.
    Also remove `startAfterAt` from the file header comment at line 7 that lists UTC ISO fields.

    EDIT B — taskToRow input handling (around line 474–478):
    The current code is:
      if (task.startAfterAt !== undefined) {
        row.start_after_at = task.startAfterAt || null;
      } else if (task.startAfter !== undefined) {
        row.start_after_at = task.startAfter ? toDateISO(task.startAfter) || null : null;
      }
    Replace this with the `else if` branch only, dropping the `startAfterAt` check entirely:
      if (task.startAfter !== undefined) {
        row.start_after_at = task.startAfter ? toDateISO(task.startAfter) || null : null;
      }
    This means if a client sends `startAfterAt` it is silently ignored rather than applied. The `startAfter` path (date key) is the only remaining input route to `start_after_at`.
  </action>

  <verify>
    <automated>grep -n "startAfterAt" juggler-backend/src/controllers/task.controller.js</automated>
  </verify>

  <done>grep returns zero lines — the property name `startAfterAt` no longer appears anywhere in task.controller.js.</done>
</task>

<task type="auto">
  <name>Task 2: Remove startAfterAt from MCP input schema and run tests</name>
  <files>juggler-backend/src/mcp/tools/tasks.js</files>

  <action>
    EDIT — mcp/tools/tasks.js (around line 39):
    Delete the single line:
      startAfterAt: z.string().optional().describe('Start-after as ISO string. AVOID — use startAfter instead.'),
    The `startAfter` entry on the line above (line 36) stays untouched.
    Also update the file's header comment (line 5) if it lists `startAfterAt` as an accepted field — remove only the `startAfterAt` mention, leaving `startAfter` intact.

    After both file edits are complete, run the backend test suite:
      cd juggler-backend && npm test

    If any test fails because it asserts that a task object contains `startAfterAt`, update that assertion to either assert the property is absent or remove the assertion. Do not add `startAfterAt` back to the code to make tests pass.
  </action>

  <verify>
    <automated>cd juggler-backend && npm test 2>&1 | tail -20</automated>
  </verify>

  <done>grep returns zero lines for startAfterAt in mcp/tools/tasks.js; npm test exits 0 with no failures related to startAfterAt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → tasks.js schema | Clients sending startAfterAt are now silently ignored at taskToRow; no error thrown |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | taskToRow: client sends startAfterAt to set start_after_at | mitigate | Remove the startAfterAt input branch entirely; field is silently ignored, preventing accidental writes via the deprecated alias |
| T-10-02 | Information Disclosure | rowToTask: startAfterAt exposed task object internals | mitigate | Removing the field reduces the task object surface area |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
After edits:
- `grep -n "startAfterAt" juggler-backend/src/controllers/task.controller.js` → 0 lines
- `grep -n "startAfterAt" juggler-backend/src/mcp/tools/tasks.js` → 0 lines
- `grep -n "startAfter:" juggler-backend/src/controllers/task.controller.js` → exactly one hit: `startAfter: startAfter` in the rowToTask return object
- `cd juggler-backend && npm test` → exits 0
</verification>

<success_criteria>
`startAfterAt` does not appear in any task object returned by rowToTask. The MCP schema no longer lists it as an accepted input. `startAfter` (date key) and start_after_at (DB column) are fully intact. All backend tests pass.
</success_criteria>

<output>
Create `.planning/phases/10-drop-startAfterAt/10-01-SUMMARY.md` when done
</output>
