---
phase: 11-migrate-rigid-to-placementMode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - juggler-backend/src/scheduler/runSchedule.js
  - juggler-backend/src/controllers/cal-sync.controller.js
  - juggler-backend/src/controllers/data.controller.js
  - juggler-backend/src/controllers/task.controller.js
  - juggler-backend/src/scripts/register-plans.js
  - juggler-mcp/index.js
autonomous: true
requirements:
  - RIGID-READS
  - RIGID-WRITES
  - RIGID-MCP
  - RIGID-ROWTO-TASK
must_haves:
  truths:
    - "No file in juggler-backend/src/ reads task.rigid, t.rigid, or original.rigid after this plan"
    - "register-plans.js uses placementMode: 'fixed' not rigid: true at all 5 call sites"
    - "cal-sync isFixed check uses task.placementMode === 'fixed' only — no when.indexOf('fixed') and no task.rigid"
    - "data.controller.js exports placementMode not rigid"
    - "rowToTask in task.controller.js does not emit a rigid property"
    - "juggler-mcp/index.js has no rigid field in any zod schema"
    - "npm test passes with no new failures"
  artifacts:
    - path: "juggler-backend/src/scheduler/runSchedule.js"
      provides: "Chunk copy and recurring-skip guard use placementMode === 'fixed'"
      contains: "placementMode === 'fixed'"
    - path: "juggler-backend/src/controllers/cal-sync.controller.js"
      provides: "isFixed check uses placementMode === 'fixed' only"
      contains: "placementMode === 'fixed'"
    - path: "juggler-backend/src/controllers/data.controller.js"
      provides: "Export row uses placementMode not rigid"
      contains: "placementMode"
    - path: "juggler-backend/src/controllers/task.controller.js"
      provides: "rowToTask emits no rigid property"
    - path: "juggler-backend/src/scripts/register-plans.js"
      provides: "All 5 task-creation blocks use placementMode: 'fixed'"
      contains: "placementMode: 'fixed'"
    - path: "juggler-mcp/index.js"
      provides: "No rigid field in zod schemas"
  key_links:
    - from: "runSchedule.js line 904"
      to: "chunk object"
      via: "placementMode: master.placementMode"
      pattern: "placementMode.*master\\.placementMode"
    - from: "runSchedule.js line 1194"
      to: "recurring skip guard"
      via: "original.placementMode === 'fixed'"
      pattern: "placementMode.*fixed"
    - from: "cal-sync.controller.js line 866"
      to: "isFixed variable"
      via: "task.placementMode === 'fixed'"
      pattern: "task\\.placementMode.*fixed"
---

<objective>
Replace every JavaScript read and write of the `rigid` boolean property with direct `placementMode === 'fixed'` checks.

Purpose: `rigid` was a backward-compat virtual column computed by the DB view. After plan 02 drops it from the view, any remaining JS code that reads `task.rigid` will receive `undefined` and silently break. This plan eliminates all reads, writes, and schema declarations of `rigid` before the view column is dropped.
Output: Six modified files with no remaining `rigid` references (other than comments explaining the removal).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@juggler/.planning/ROADMAP.md
@juggler-backend/src/lib/placementModes.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace rigid reads in runSchedule.js and cal-sync.controller.js</name>
  <files>
    juggler-backend/src/scheduler/runSchedule.js
    juggler-backend/src/controllers/cal-sync.controller.js
  </files>

  <read_first>
    - juggler-backend/src/scheduler/runSchedule.js lines 895–915 — chunk-copy block containing `rigid: master.rigid`
    - juggler-backend/src/scheduler/runSchedule.js lines 1185–1205 — recurring-skip guard containing `original.rigid`
    - juggler-backend/src/controllers/cal-sync.controller.js lines 858–875 — isFixed block containing the stale when.indexOf check and task.rigid read
  </read_first>

  <action>
    runSchedule.js — two changes:

    1. Line ~904: Change `rigid: master.rigid` to `placementMode: master.placementMode`.
       The chunk object is copying properties from the master task. `rigid` is being replaced
       by `placementMode` as the authoritative field. Do not add a `rigid` alias — remove it
       entirely.

    2. Line ~1194: Change `original.recurring && original.rigid && !dateChanged` to
       `original.recurring && original.placementMode === 'fixed' && !dateChanged`.
       The semantics are identical — this guard prevents rewriting the scheduled time of a
       recurring fixed task when only the date hasn't changed. Use the PLACEMENT_MODES.FIXED
       constant if it is already imported in this file; otherwise use the string literal
       `'fixed'` and add a comment: `// 'fixed' per placementModes.js PLACEMENT_MODES.FIXED`.

    cal-sync.controller.js — one change:

    Line ~866: The full existing expression is:
      `var isFixed = (task.when || '').indexOf('fixed') >= 0 || task.rigid;`
    Replace the entire right-hand side with:
      `const isFixed = task.placementMode === 'fixed';`
    The `when.indexOf('fixed')` part is dead code — phase 09 stripped 'fixed' tokens from
    the `when` column. The `task.rigid` read is what this plan eliminates. The new single
    expression is the canonical check. Change `var` to `const` while editing.
  </action>

  <verify>
    <automated>cd "/Users/david/Offline Coding/Raike & Sons/juggler/juggler-backend" && grep -n "\.rigid" src/scheduler/runSchedule.js src/controllers/cal-sync.controller.js</automated>
  </verify>

  <done>grep returns no matches for `.rigid` in either file. The two runSchedule.js changes use `placementMode` property access. The cal-sync isFixed line reads `task.placementMode === 'fixed'` only.</done>
</task>

<task type="auto">
  <name>Task 2: Replace rigid in data.controller.js, task.controller.js, register-plans.js, and juggler-mcp/index.js</name>
  <files>
    juggler-backend/src/controllers/data.controller.js
    juggler-backend/src/controllers/task.controller.js
    juggler-backend/src/scripts/register-plans.js
    juggler-mcp/index.js
  </files>

  <read_first>
    - juggler-backend/src/controllers/data.controller.js lines 105–120 — export row containing `rigid: t.rigid ? 1 : 0`
    - juggler-backend/src/controllers/task.controller.js lines 390–410 — rowToTask function containing `rigid: !!row.rigid`
    - juggler-backend/src/scripts/register-plans.js lines 55–75, 110–125, 170–185, 225–240, 280–295 — the 5 task-creation blocks each containing `rigid: true`
    - juggler-mcp/index.js lines 95–115, 135–150, 170–190, 320–335 — the 4 zod schema blocks each containing `rigid: z.boolean().optional()`
  </read_first>

  <action>
    data.controller.js — line ~112:
    Change `rigid: t.rigid ? 1 : 0` to `placementMode: t.placementMode`.
    The export is serializing task properties. Downstream importers (if any) will receive
    `placementMode` as a string instead of the legacy boolean-int `rigid`. This is a
    breaking change to the export format but is intentional — the export format should
    reflect the authoritative schema.

    task.controller.js — rowToTask function, line ~399:
    Remove the line `rigid: !!row.rigid` entirely. Do not replace it with anything.
    The `rowToTask` function maps DB row columns to task objects. The `rigid` virtual
    column is being dropped from the view in plan 02. Removing it here means the property
    will simply not appear on task objects returned by rowToTask — callers that needed
    a rigidity check should already use `task.placementMode === 'fixed'` (the scheduler
    already does per the research notes). Scan the surrounding lines (~395–415) and confirm
    no other reference to `rigid` exists in rowToTask before removing.

    register-plans.js — 5 task-creation blocks at lines ~64, ~118, ~178, ~232, ~287:
    In each block, remove the line `rigid: true` and add `placementMode: 'fixed'` in its
    place. Each block is a task-creation payload sent to the tasks API or DB. These tasks
    are hard-coded as fixed-time plan items. The replacement is semantically identical.
    Verify that `placementMode` is not already present in any of these blocks before adding
    it — if it is, simply remove `rigid: true` and leave `placementMode` as-is.

    juggler-mcp/index.js — 4 zod schema blocks at lines ~104, ~144, ~181, ~328:
    Remove `rigid: z.boolean().optional()` from each schema block. Do not add a
    `placementMode` field here — the MCP input schema already accepts `placementMode` as
    a string field (verify by grepping for `placementMode` in index.js before editing;
    if absent, add `placementMode: z.string().optional()` to each schema block instead of
    just removing `rigid`). The MCP consumer should pass `placementMode: 'fixed'` rather
    than `rigid: true`.
  </action>

  <verify>
    <automated>cd "/Users/david/Offline Coding/Raike & Sons/juggler/juggler-backend" && grep -rn "\.rigid\b\|rigid:" src/controllers/data.controller.js src/controllers/task.controller.js src/scripts/register-plans.js && grep -n "rigid" ../juggler-mcp/index.js | grep -v node_modules | grep -v "type-checking"</automated>
  </verify>

  <done>
    No remaining `rigid` property reads or writes in any of the four files (ignoring node_modules and comments about the removal). register-plans.js has `placementMode: 'fixed'` at all 5 call sites. juggler-mcp/index.js has no `rigid: z.boolean()` schema fields.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify no remaining rigid reads across all juggler-backend source files and run tests</name>
  <files></files>

  <action>
    Run a broad grep across all juggler-backend/src/ JS files to confirm no remaining
    `task.rigid`, `t.rigid`, `u.rigid`, `master.rigid`, `original.rigid`, or `rigid: true`
    references exist. If any remain, fix them before proceeding to the test run.

    Then run the full test suite to confirm no regressions.
  </action>

  <verify>
    <automated>cd "/Users/david/Offline Coding/Raike & Sons/juggler/juggler-backend" && grep -rn --include="*.js" "\.rigid\b\|rigid: true\|rigid:true" src/ | grep -v "node_modules" | grep -v "\.test\." || echo "CLEAN — no rigid reads found"</automated>
    <automated>cd "/Users/david/Offline Coding/Raike & Sons/juggler/juggler-backend" && npm test 2>&1 | tail -20</automated>
  </verify>

  <done>grep returns "CLEAN — no rigid reads found" (or zero matches). `npm test` exits 0 with no new failures compared to pre-phase baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JS source → DB view | After plan 02 drops the rigid column, any JS code that reads row.rigid will silently receive undefined — eliminated in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | cal-sync isFixed logic | mitigate | Remove both dead conditions (when.indexOf + task.rigid) in one edit; grep verify confirms single canonical check remains |
| T-11-02 | Tampering | data export format change (rigid→placementMode) | accept | No real users yet; export format change is intentional and documented in plan action |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
After plan 01 completes:
- `grep -rn "\.rigid" juggler-backend/src/ juggler-mcp/index.js` returns zero matches (excluding node_modules and test fixtures)
- `grep -rn "rigid: true" juggler-backend/src/scripts/register-plans.js` returns zero matches
- `grep -n "placementMode: 'fixed'" juggler-backend/src/scripts/register-plans.js | wc -l` returns 5
- `npm test` passes
</verification>

<success_criteria>
All JavaScript reads and writes of the `rigid` property have been removed from juggler-backend/src/ and juggler-mcp/index.js. The codebase exclusively uses `placementMode === 'fixed'` for rigidity checks. Tests pass. The codebase is ready for plan 02 to drop the view column without breaking anything.
</success_criteria>

<output>
Create `.planning/phases/11-migrate-rigid-to-placementMode/11-01-SUMMARY.md` when done
</output>
