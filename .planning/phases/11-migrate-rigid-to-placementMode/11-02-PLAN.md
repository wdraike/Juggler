---
phase: 11-migrate-rigid-to-placementMode
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - juggler-backend/src/db/migrations/20260518000200_drop_rigid_from_views.js
autonomous: true
requirements:
  - RIGID-VIEW-DROP
must_haves:
  truths:
    - "tasks_v does not have a rigid column after migration runs"
    - "tasks_with_sync_v does not have a rigid column after migration runs"
    - "Both views still exist and return all expected non-rigid columns"
    - "npm test passes after migration"
  artifacts:
    - path: "juggler-backend/src/db/migrations/20260518000200_drop_rigid_from_views.js"
      provides: "Knex migration that rebuilds both views without the rigid computed column"
      contains: "exports.up"
  key_links:
    - from: "migration DROP VIEW"
      to: "tasks_v CREATE VIEW"
      via: "Rebuilt with no rigid CASE expression"
      pattern: "CREATE.*VIEW.*tasks_v"
    - from: "tasks_v"
      to: "tasks_with_sync_v"
      via: "tasks_with_sync_v selects from tasks_v — drop tasks_with_sync_v first"
      pattern: "DROP VIEW IF EXISTS tasks_with_sync_v"
---

<objective>
Write and run the Knex migration that drops the `rigid` virtual column from both `tasks_v` and `tasks_with_sync_v` database views.

Purpose: The `rigid` column was a backward-compat alias computed as `CASE WHEN m.placement_mode = 'fixed' THEN 1 ELSE 0 END AS rigid`. All JavaScript consumers have been removed in plan 01. Keeping the column in the view is dead weight and will mislead future readers. This migration rebuilds both views without the `rigid` column.
Output: A single migration file. After `npx knex migrate:latest` runs, `SELECT rigid FROM tasks_v` throws "Unknown column" — confirming removal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@juggler-backend/src/db/migrations/20260518000100_placement_mode_enum_redesign.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 20260518000200_drop_rigid_from_views.js</name>
  <files>juggler-backend/src/db/migrations/20260518000200_drop_rigid_from_views.js</files>

  <read_first>
    - juggler-backend/src/db/migrations/20260518000100_placement_mode_enum_redesign.js — full CREATE VIEW SQL for both tasks_v and tasks_with_sync_v as rebuilt by phase 09; this is the base to copy and edit (remove only the rigid CASE line)
  </read_first>

  <action>
    Create the migration file at `juggler-backend/src/db/migrations/20260518000200_drop_rigid_from_views.js`.

    The `up` function must run inside `knex.transaction(async (trx) => { ... })`:

    STEP 1 — Drop views in dependency order (tasks_with_sync_v depends on tasks_v):
      await trx.raw('DROP VIEW IF EXISTS tasks_with_sync_v');
      await trx.raw('DROP VIEW IF EXISTS tasks_v');

    STEP 2 — Recreate tasks_v:
    Copy the full CREATE VIEW tasks_v SQL from migration 20260518000100 verbatim, then
    make exactly ONE change: remove the line that computes the `rigid` column:
      CASE WHEN m.placement_mode = 'fixed' THEN 1 ELSE 0 END AS rigid
    This line appears twice in tasks_v (once in the recurring-template SELECT branch and
    once in the instances SELECT branch — both UNION arms). Remove both occurrences.
    Do not alter any other column. Do not remove the `marker` column — that is a separate
    virtual column that stays.
    Fix trailing commas if removing the `rigid` line leaves a dangling comma on the
    preceding column in either SELECT branch.

    STEP 3 — Recreate tasks_with_sync_v:
    Copy the full CREATE VIEW tasks_with_sync_v SQL from migration 20260518000100
    verbatim, without any changes. This view selects from tasks_v, so once tasks_v no
    longer has `rigid`, tasks_with_sync_v automatically loses it too — no edits needed
    to its SQL.

    The `down` function should throw:
      throw new Error('Down migration for drop_rigid_from_views not implemented — '
        + 'rigid column was a legacy alias; re-adding it is intentional regression.');
  </action>

  <verify>
    <automated>cd "/Users/david/Offline Coding/Raike & Sons/juggler/juggler-backend" && npx knex migrate:latest 2>&1</automated>
    <automated>cd "/Users/david/Offline Coding/Raike & Sons/juggler/juggler-backend" && node -e "
      const knex = require('./src/db');
      knex.raw('SELECT rigid FROM tasks_v LIMIT 1')
        .then(() => { console.log('FAIL — rigid column still exists'); return knex.destroy(); })
        .catch(e => {
          if (e.message.includes('Unknown column')) {
            console.log('PASS — rigid column not found in tasks_v');
          } else {
            console.error('UNEXPECTED ERROR:', e.message);
            process.exit(1);
          }
          return knex.destroy();
        });
    "
    </automated>
    <automated>cd "/Users/david/Offline Coding/Raike & Sons/juggler/juggler-backend" && node -e "
      const knex = require('./src/db');
      knex.raw('SELECT marker, placement_mode FROM tasks_v LIMIT 3')
        .then(([rows]) => { console.log('tasks_v columns intact:', JSON.stringify(rows)); return knex.destroy(); })
        .catch(e => { console.error('FAIL:', e.message); process.exit(1); });
    "
    </automated>
  </verify>

  <done>
    `npx knex migrate:latest` runs without error. Querying `rigid` from tasks_v returns "Unknown column" error (column removed). Querying `marker` and `placement_mode` from tasks_v succeeds (view intact). Both views exist.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run tests to confirm no regressions from view change</name>
  <files></files>

  <action>
    Run the full test suite. The migration has already run (Task 1). Confirm no test reads
    `task.rigid` from the view output — any test that does will now fail with undefined/0
    instead of the expected value, which is the correct failure mode that this phase was
    designed to catch and eliminate.

    If any test failures mention `rigid`, inspect each one: either the test was testing
    legacy behavior (update it to test `placementMode === 'fixed'`) or a JS rigid read was
    missed by plan 01 (fix the source file). Do not suppress test failures.
  </action>

  <verify>
    <automated>cd "/Users/david/Offline Coding/Raike & Sons/juggler/juggler-backend" && npm test 2>&1 | tail -30</automated>
  </verify>

  <done>npm test exits 0. No test failures mention `rigid`. The views are clean and the JS layer is aligned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration → MySQL views | DDL runs with DB credentials; SQL is hardcoded, not user-supplied — no injection vector |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | Tampering | View rebuild introduces column-order drift | mitigate | Copy CREATE VIEW SQL verbatim from prior migration; remove only the rigid line; verify marker + placement_mode still queryable after migration |
| T-11-02-02 | Denial of Service | DROP VIEW on tasks_with_sync_v before tasks_v | mitigate | Drop tasks_with_sync_v first (depends on tasks_v); both drops inside transaction so partial failure rolls back |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
After plan 02 completes:
- `SELECT rigid FROM tasks_v LIMIT 1` → MySQL error "Unknown column 'rigid' in 'field list'"
- `SELECT rigid FROM tasks_with_sync_v LIMIT 1` → same error
- `SELECT marker, placement_mode, id FROM tasks_v LIMIT 1` → returns rows without error
- `npm test` passes
- `grep -rn "\.rigid\b\|rigid:" juggler-backend/src/` returns zero matches (phase complete)
</verification>

<success_criteria>
The `rigid` virtual column is gone from both DB views. All JS rigid reads were removed in plan 01. The full test suite passes. No consumer in juggler-backend/src/ or juggler-mcp/ reads or writes `rigid`. The migration is idempotent (re-running migrate:latest is a no-op after first run).
</success_criteria>

<output>
Create `.planning/phases/11-migrate-rigid-to-placementMode/11-02-SUMMARY.md` when done
</output>
