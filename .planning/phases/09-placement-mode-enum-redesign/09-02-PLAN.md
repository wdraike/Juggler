---
phase: 09-placement-mode-enum-redesign
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - juggler-backend/src/lib/placementModes.js
autonomous: true
requirements:
  - PM-ENUM-CONSTANTS
must_haves:
  truths:
    - "PLACEMENT_MODES has exactly 6 keys: REMINDER, ALL_DAY, FIXED, TIME_WINDOW, TIME_BLOCKS, ANYTIME"
    - "No key named MARKER, FLEXIBLE, PINNED_DATE, RECURRING_RIGID, RECURRING_WINDOW, or RECURRING_FLEXIBLE exists"
    - "Values match the DB enum strings exactly (reminder, all_day, fixed, time_window, time_blocks, anytime)"
  artifacts:
    - path: "juggler-backend/src/lib/placementModes.js"
      provides: "Shared constants for placement_mode values"
      exports: ["PLACEMENT_MODES"]
  key_links:
    - from: "juggler-backend/src/lib/placementModes.js"
      to: "juggler-backend/src/controllers/task.controller.js"
      via: "require('../lib/placementModes')"
      pattern: "PLACEMENT_MODES"
    - from: "juggler-backend/src/lib/placementModes.js"
      to: "juggler-backend/src/scheduler/unifiedScheduleV2.js"
      via: "require('../lib/placementModes')"
      pattern: "PLACEMENT_MODES"
---

<objective>
Replace the 7-value PLACEMENT_MODES constant object with the new 6-value object.

Purpose: This is the shared source-of-truth for all enum strings. Changing it here causes Wave 2 plans (controller + scheduler) to fail compilation/lint if they reference any deleted key — making stale references detectable before runtime.
Output: Updated placementModes.js with only the 6 new constants.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@juggler-backend/src/lib/placementModes.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace PLACEMENT_MODES object with 6 new values</name>
  <files>juggler-backend/src/lib/placementModes.js</files>

  <read_first>
    - juggler-backend/src/lib/placementModes.js — current 7-value object to replace
  </read_first>

  <action>
    Overwrite the entire file. The new content must define PLACEMENT_MODES as:
      REMINDER:    'reminder'
      ALL_DAY:     'all_day'
      FIXED:       'fixed'
      TIME_WINDOW: 'time_window'
      TIME_BLOCKS: 'time_blocks'
      ANYTIME:     'anytime'
    Export as module.exports = { PLACEMENT_MODES }.
    No other exports, no other keys, no backward-compat aliases.
    Add a brief JSDoc comment above the object noting the values match the
    task_masters.placement_mode ENUM column and migration 20260518000100.
    Do NOT add a FLEXIBLE, MARKER, RECURRING_RIGID, RECURRING_WINDOW,
    RECURRING_FLEXIBLE, or PINNED_DATE key — these are intentionally removed
    per D-01 through D-04.
  </action>

  <verify>
    <automated>node -e "var {PLACEMENT_MODES}=require('./juggler-backend/src/lib/placementModes'); var keys=Object.keys(PLACEMENT_MODES); var expected=['REMINDER','ALL_DAY','FIXED','TIME_WINDOW','TIME_BLOCKS','ANYTIME']; var ok=keys.length===6&&expected.every(k=>PLACEMENT_MODES[k]); console.log(ok?'PASS':'FAIL',JSON.stringify(PLACEMENT_MODES));"</automated>
  </verify>

  <acceptance_criteria>
    - File contains exactly 6 keys: REMINDER, ALL_DAY, FIXED, TIME_WINDOW, TIME_BLOCKS, ANYTIME
    - PLACEMENT_MODES.REMINDER === 'reminder'
    - PLACEMENT_MODES.ALL_DAY === 'all_day'
    - PLACEMENT_MODES.FIXED === 'fixed'
    - PLACEMENT_MODES.TIME_WINDOW === 'time_window'
    - PLACEMENT_MODES.TIME_BLOCKS === 'time_blocks'
    - PLACEMENT_MODES.ANYTIME === 'anytime'
    - No key named FLEXIBLE, MARKER, PINNED_DATE, RECURRING_RIGID, RECURRING_WINDOW, or RECURRING_FLEXIBLE exists
    - `module.exports = { PLACEMENT_MODES }` is the only export
  </acceptance_criteria>

  <done>placementModes.js exports exactly 6 constants matching the new DB ENUM values. The node verify command prints PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| constants module → consumers | Constants are compile-time strings; no runtime input validation needed here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-SC | Tampering | npm installs | accept | No package installs in this plan |
</threat_model>

<verification>
`node -e "var {PLACEMENT_MODES}=require('./juggler-backend/src/lib/placementModes'); console.log(Object.keys(PLACEMENT_MODES).length)"` prints 6.
`grep -c "FLEXIBLE\|MARKER\|PINNED_DATE\|RECURRING" juggler-backend/src/lib/placementModes.js` prints 0.
</verification>

<success_criteria>
placementModes.js contains exactly the 6 new constants with the correct string values. No old constants remain.
</success_criteria>

<output>
Create `.planning/phases/09-placement-mode-enum-redesign/09-02-SUMMARY.md` when done
</output>
