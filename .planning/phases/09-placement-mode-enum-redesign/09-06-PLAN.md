---
phase: 09-placement-mode-enum-redesign
plan: 06
type: execute
wave: 4
depends_on:
  - 09-03
  - 09-04
  - 09-05
files_modified:
  - juggler-backend/docs/TASK-PROPERTIES.md
  - juggler-backend/docs/SCHEDULER.md
  - juggler-backend/tests/unit/placement-mode-migration.test.js
autonomous: true
requirements:
  - PM-ENUM-DOCS
  - PM-ENUM-TESTS
must_haves:
  truths:
    - "TASK-PROPERTIES.md lists the 6 new placement_mode values with correct descriptions"
    - "TASK-PROPERTIES.md no longer lists marker, rigid, FLEXIBLE, RECURRING_RIGID, etc. as mode triggers"
    - "SCHEDULER.md placement branching section describes placement_mode-first branching"
    - "Unit test verifies the backfill CASE expression maps all 7 old values to the correct 6 new values"
    - "Unit test verifies that 'allday' and 'fixed' tokens are stripped from the when column"
  artifacts:
    - path: "juggler-backend/docs/TASK-PROPERTIES.md"
      provides: "Task property reference documentation"
      contains: "placement_mode"
    - path: "juggler-backend/docs/SCHEDULER.md"
      provides: "Scheduler design documentation"
      contains: "placement_mode"
    - path: "juggler-backend/tests/unit/placement-mode-migration.test.js"
      provides: "Unit tests for migration backfill logic"
      contains: "describe('placement mode backfill'"
  key_links:
    - from: "TASK-PROPERTIES.md Scheduling Modes table"
      to: "6 new placement_mode values"
      via: "table rows listing each mode"
      pattern: "placement_mode"
    - from: "SCHEDULER.md section 4a"
      to: "placement_mode enum values"
      via: "buildItems early-return branching description"
      pattern: "placement_mode"
---

<objective>
Update docs to reflect the new 6-value placement_mode system and write a unit test that verifies the migration backfill mapping logic.

Purpose: Documentation must stay accurate or future developers will implement against the old model. The unit test catches any logic bugs in the CASE expression backfill before production runs — it is the only way to verify the migration without running it against production data.
Output: Updated TASK-PROPERTIES.md, updated SCHEDULER.md placement section, and a standalone unit test for the backfill mapping.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@juggler-backend/docs/TASK-PROPERTIES.md
@juggler-backend/docs/SCHEDULER.md
@juggler-backend/src/db/migrations/20260518000100_placement_mode_enum_redesign.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update TASK-PROPERTIES.md — replace scheduling modes table and property rows</name>
  <files>juggler-backend/docs/TASK-PROPERTIES.md</files>

  <read_first>
    - juggler-backend/docs/TASK-PROPERTIES.md — read the full file (139 lines); identify every reference to old mode names: FLEXIBLE, MARKER, RECURRING_RIGID, RECURRING_WINDOW, RECURRING_FLEXIBLE, when='fixed', when='allday'
    - juggler-backend/src/db/migrations/20260518000100_placement_mode_enum_redesign.js — to confirm the 6 new values and their semantics from the CONTEXT.md decisions
  </read_first>

  <action>
    Make these targeted edits to TASK-PROPERTIES.md:

    EDIT 1 — Replace the "Scheduling Modes" table (lines 8-19):
    Replace the Mode/Trigger/Phase/Behavior table with a new table showing the 6
    placement_mode values. The table should have columns: Mode / Value / Scheduler treatment.
    Rows:
      reminder   | 'reminder'    | Calendar marker. dur=0. Coexists with other tasks at same minute. No time-grid occupancy.
      all_day    | 'all_day'     | Spans full day. Excluded from time-grid placement entirely (early return in buildItems).
      fixed      | 'fixed'       | Immovable at exact time (from `time` field). Blocks the slot. date_pinned tasks also treated as fixed-anchor.
      time_window| 'time_window' | Placed within ±timeFlex minutes of preferredTimeMins. Falls back to when-tags if window is degenerate.
      time_blocks| 'time_blocks' | Constrained to user-named `when` tag windows only. Uses flexWhen for retry.
      anytime    | 'anytime'     | No constraint. Placed wherever fits by priority/slack order.

    Add a note: "Recurrence is orthogonal — any mode can be recurring. Use the `recurring` flag to check if a task is recurring; do not infer from placement_mode."

    EDIT 2 — Update When column description (line 62):
    The "When" property row currently says `when` can contain 'fixed' and 'allday'. Change the
    Scheduler Effect column to:
    "Comma-separated user-defined time block tags (e.g. morning, lunch, evening). Empty = all windows. Must NOT contain 'allday' or 'fixed' — these are now expressed via placement_mode."

    EDIT 3 — Update Rigid row (line 64):
    The `rigid` row currently says "Forces placement at exact preferredTimeMins. Phase 0 only."
    Add: "Note: rigid is now a UI-level toggle within the time_window mode. The scheduler reads isRigid from placement_mode === 'fixed' — do not use the rigid flag for scheduler branching."

    EDIT 4 — Update Marker row (line 103):
    The `marker` bool row says "Non-blocking reminder. Shown on calendar, doesn't consume time."
    Add: "Expressed as placement_mode = 'reminder' in the DB. The `marker` computed column in tasks_v is CASE WHEN placement_mode = 'reminder' THEN 1 ELSE 0 END."

    EDIT 5 — Add placement_mode property row:
    In the "When & Where" section (lines 58-71), add a new row for placement_mode:
      | Placement Mode | `placement_mode` | `placementMode` | enum (see Scheduling Modes table) | User/System | Primary scheduling constraint. Written by the UI directly. Never derived server-side. |

    EDIT 6 — Remove any references to FLEXIBLE, RECURRING_RIGID, RECURRING_WINDOW, RECURRING_FLEXIBLE, PINNED_DATE, MARKER (as enum value) from the body of the document.
  </action>

  <verify>
    <automated>grep -c "FLEXIBLE\|RECURRING_RIGID\|RECURRING_WINDOW\|RECURRING_FLEXIBLE\|PINNED_DATE\|placement_mode = 'marker'\|placement_mode = 'flexible'" juggler-backend/docs/TASK-PROPERTIES.md</automated>
    Must return 0.
    <automated>grep -c "reminder\|all_day\|time_window\|time_blocks\|anytime" juggler-backend/docs/TASK-PROPERTIES.md</automated>
    Must return at least 6 (one per new mode value).
  </verify>

  <acceptance_criteria>
    - `grep -c "FLEXIBLE\|RECURRING_RIGID\|RECURRING_WINDOW\|RECURRING_FLEXIBLE\|PINNED_DATE" juggler-backend/docs/TASK-PROPERTIES.md` returns 0
    - The Scheduling Modes table contains exactly 6 rows: reminder, all_day, fixed, time_window, time_blocks, anytime
    - The When property row no longer lists 'fixed' and 'allday' as valid when values
    - A placement_mode property row exists in the When & Where section
    - The note about recurrence orthogonality is present
  </acceptance_criteria>

  <done>TASK-PROPERTIES.md updated with 6-value placement_mode table, correct When property description, placement_mode property row added, all old mode names removed.</done>
</task>

<task type="auto">
  <name>Task 2: Update SCHEDULER.md — placement branching section (D-33)</name>
  <files>juggler-backend/docs/SCHEDULER.md</files>

  <read_first>
    - juggler-backend/docs/SCHEDULER.md — read lines 59-115 (section 4 sub-phases: 4a Immovable first through section 5 Overlap prevention); identify every reference to old mode names, when='fixed'/'allday' content detection, or RECURRING_RIGID/FLEXIBLE/MARKER terminology in the placement description
    - juggler-backend/src/lib/placementModes.js — confirm the 6 new constant names for use in doc prose
  </read_first>

  <action>
    Make these targeted edits to SCHEDULER.md per D-33:

    EDIT 1 — Update section 4a "Immovable first" (lines 65-67):
    The current text reads:
      #### 4a. Immovable first  *(Phase 0)*
      - Pinned tasks (`datePinned` + has time) at their locked times
      - Markers at their times (no occupancy)
    Replace with:
      #### 4a. Immovable first  *(Phase 0)*
      buildItems applies placement_mode-first branching before any phase runs:
      - `placement_mode = 'all_day'` → task is excluded from the time grid entirely (early return). Shown on calendar header row; does not consume minute-level capacity.
      - `placement_mode = 'reminder'` → placed at its exact time with no occupancy. Multiple reminders at the same minute do not conflict.
      - `placement_mode = 'fixed'` → anchored at the exact `time` value. isRigid=true. Blocks the slot. Also applies to tasks with `date_pinned=1`.
      - `placement_mode = 'time_window'` → placed within ±timeFlex minutes of `preferredTimeMins`. Falls back to when-tag windows if the window is degenerate.
      - `placement_mode = 'time_blocks'` → constrained to user-defined `when` tag windows (e.g. morning, lunch, evening). Uses `flexWhen` for retry when windows are full.
      - `placement_mode = 'anytime'` → no time constraint. Placed wherever fits by priority/slack order (recurring tasks with this mode can use preferLatestSlot when their anchor has already passed today).

      The `when` column contains only user-defined tag names after the Phase 9 migration. 'fixed' and 'allday' tokens are no longer stored in `when` — these are expressed as placement_mode values.

      Recurrence is orthogonal to placement_mode. Any mode can be recurring. The scheduler reads `t.recurring` (boolean flag) to determine recurrence — it does not infer recurrence from placement_mode.

    EDIT 2 — Remove old mode references from the 4b recurring section header (lines 69-72):
    If the 4b section references RECURRING_RIGID, RECURRING_WINDOW, or RECURRING_FLEXIBLE
    by name, replace them with the appropriate description (e.g. "recurring tasks with
    placement_mode = 'fixed'" instead of RECURRING_RIGID). If no such references exist,
    skip this edit.

    EDIT 3 — Update any summary table or legend that lists old ENUM values:
    Search the document for any table or list that enumerates placement_mode values as
    RECURRING_RIGID / RECURRING_WINDOW / RECURRING_FLEXIBLE / FLEXIBLE / MARKER / PINNED_DATE
    and replace with the 6 new values. If none exist beyond section 4a, skip this edit.
  </action>

  <verify>
    <automated>grep -c "RECURRING_RIGID\|RECURRING_WINDOW\|RECURRING_FLEXIBLE\|PLACEMENT_MODES.FLEXIBLE\|PLACEMENT_MODES.MARKER\|PINNED_DATE" juggler-backend/docs/SCHEDULER.md</automated>
    Must return 0 (old mode names purged from docs).
    <automated>grep -c "placement_mode" juggler-backend/docs/SCHEDULER.md</automated>
    Must return at least 6 (section 4a mentions each of the 6 new values).
  </verify>

  <acceptance_criteria>
    - `grep -c "RECURRING_RIGID\|RECURRING_WINDOW\|RECURRING_FLEXIBLE" juggler-backend/docs/SCHEDULER.md` returns 0
    - Section 4a describes the 6 placement_mode values and their buildItems treatment
    - Section 4a states that 'fixed' and 'allday' tokens are no longer stored in the when column
    - Section 4a states that recurrence is orthogonal to placement_mode (t.recurring is the flag)
    - `grep -c "placement_mode" juggler-backend/docs/SCHEDULER.md` returns at least 6
  </acceptance_criteria>

  <done>SCHEDULER.md section 4a updated to describe placement_mode-first branching with all 6 new values. Old mode names removed. Recurrence orthogonality documented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write unit test for migration backfill mapping and when-strip logic</name>
  <files>juggler-backend/tests/unit/placement-mode-migration.test.js</files>

  <read_first>
    - juggler-backend/src/db/migrations/20260518000100_placement_mode_enum_redesign.js — read the backfill CASE expression and the when-strip logic to test against
    - juggler-backend/tests/ — check for any existing test runner config (jest.config.js, package.json test script) to confirm test file naming convention
  </read_first>

  <behavior>
    - Test: marker rows map to 'reminder'
      Input: {placement_mode: 'marker', when: null, preferred_time_mins: null}
      Expected: placement_mode = 'reminder'
    - Test: when LIKE '%allday%' maps to 'all_day'
      Input: {placement_mode: 'flexible', when: 'allday', preferred_time_mins: null}
      Expected: placement_mode = 'all_day'
    - Test: when LIKE '%fixed%' maps to 'fixed'
      Input: {placement_mode: 'flexible', when: 'fixed', preferred_time_mins: null}
      Expected: placement_mode = 'fixed'
    - Test: preferred_time_mins IS NOT NULL maps to 'time_window'
      Input: {placement_mode: 'recurring_window', when: 'morning', preferred_time_mins: 480}
      Expected: placement_mode = 'time_window'
    - Test: recurring_rigid with preferredTimeMins maps to 'time_window'
      Input: {placement_mode: 'recurring_rigid', when: null, preferred_time_mins: 420}
      Expected: placement_mode = 'time_window'
    - Test: when IS NOT NULL AND != '' maps to 'time_blocks'
      Input: {placement_mode: 'recurring_flexible', when: 'morning,evening', preferred_time_mins: null}
      Expected: placement_mode = 'time_blocks'
    - Test: fallthrough maps to 'anytime'
      Input: {placement_mode: 'flexible', when: null, preferred_time_mins: null}
      Expected: placement_mode = 'anytime'
    - Test: when-strip removes 'allday' token
      Input: when = 'morning,allday,evening'
      Expected: when = 'morning,evening'
    - Test: when-strip removes 'fixed' token
      Input: when = 'fixed'
      Expected: when = null (empty string → null)
    - Test: when-strip removes both tokens from mixed value
      Input: when = 'allday,morning,fixed'
      Expected: when = 'morning'
  </behavior>

  <action>
    Create the test file as a pure unit test — do NOT connect to the DB. Extract the
    backfill CASE logic into a pure JS function `applyBackfill(row)` that mirrors the
    CASE expression, and the when-strip logic into `stripWhenTokens(when)`. Test these
    functions directly.

    Structure the test file as:
      function applyBackfill(row) { ... } // mirrors the CASE expression from the migration
      function stripWhenTokens(when) { ... } // mirrors the strip logic from the migration
      describe('placement mode backfill', function() { ... })
      describe('when token strip', function() { ... })

    Use the same test runner as the rest of the juggler-backend test suite (check package.json).
    If jest is used, use jest describe/it/expect. If mocha, use describe/it/assert.

    The applyBackfill function must implement the same CASE priority order as the migration:
    1. placement_mode === 'marker' → 'reminder'
    2. when LIKE '%allday%' → 'all_day'
    3. when LIKE '%fixed%' → 'fixed'
    4. preferred_time_mins IS NOT NULL → 'time_window'
    5. when IS NOT NULL && when !== '' → 'time_blocks'
    6. else → 'anytime'

    The stripWhenTokens function must:
    - Remove 'allday' and 'fixed' tokens from comma-separated when string
    - Collapse multiple commas
    - Return null if result is empty string
  </action>

  <verify>
    <automated>cd juggler-backend && npx jest tests/unit/placement-mode-migration.test.js --no-coverage 2>&1 | tail -10</automated>
    All tests must pass. If jest not configured, use: node --test tests/unit/placement-mode-migration.test.js
  </verify>

  <acceptance_criteria>
    - Test file exists at juggler-backend/tests/unit/placement-mode-migration.test.js
    - File contains at least 10 test cases matching the behavior block above
    - All tests pass when run
    - applyBackfill('marker' row) returns 'reminder'
    - applyBackfill(flexible row with no when, no preferredTimeMins) returns 'anytime'
    - stripWhenTokens('morning,allday,evening') returns 'morning,evening'
    - stripWhenTokens('fixed') returns null (or '')
    - stripWhenTokens('allday,morning,fixed') returns 'morning'
  </acceptance_criteria>

  <done>Unit test file exists. All 10+ cases pass. applyBackfill and stripWhenTokens correctly mirror the migration CASE logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → developers | Inaccurate docs cause future regressions; no security boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06-SC | Tampering | npm installs | accept | No new package installs; test runner already in devDependencies |
</threat_model>

<verification>
`grep -c "FLEXIBLE\|RECURRING_RIGID\|RECURRING_WINDOW\|RECURRING_FLEXIBLE\|PINNED_DATE" juggler-backend/docs/TASK-PROPERTIES.md` → 0
`grep -c "RECURRING_RIGID\|RECURRING_WINDOW\|RECURRING_FLEXIBLE" juggler-backend/docs/SCHEDULER.md` → 0
`grep -c "placement_mode" juggler-backend/docs/SCHEDULER.md` → at least 6
`cd juggler-backend && npx jest tests/unit/placement-mode-migration.test.js` → all pass
</verification>

<success_criteria>
TASK-PROPERTIES.md accurately describes the 6-value placement_mode system. SCHEDULER.md section 4a reflects placement_mode-first branching with all 6 values documented. Unit tests prove the backfill and strip logic are correct before the migration touches production data.
</success_criteria>

<output>
Create `.planning/phases/09-placement-mode-enum-redesign/09-06-SUMMARY.md` when done
</output>
