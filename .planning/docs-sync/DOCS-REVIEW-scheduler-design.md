# DOCS Review — juggler-backend/docs/architecture/SCHEDULER.md — chore —re-review — 2026-06-12

## Status: ISSUES

---

## Proof of Work

| Step | Action / command | Result |
|------|------------------|--------|
| Inputs check | Read BASE-DOCUMENTATION-RUBRIC.md + BASE-ARCH-DOC-STANDARD.md + resolved doc file | present; chore mode applies Accuracy + Structure verification |
| Doc file | SCHEDULER.md (juggler-backend architecture doc) | 1 file in scope |
| File metadata | head -15 (frontmatter): type: design, status: active, last_updated: 2026-05-25 | Frontmatter present; last_updated is 2026-05-25 (stale — today is 2026-06-12, 18 days old) |
| Architecture type | Classify per rubric §0: doc type = architecture-overview (C4 + design rationale + core algorithms) | architecture-overview type applies; required sections per BASE-ARCH-DOC-STANDARD §1–6 |
| Required sections check | Heading tree vs rubric: Core principle · Severity hierarchy · Placement phases (4a–4e) · Overlap prevention · Deep-dive 4c · Edge cases · File changes · Verification/Tests | All major sections present; no structural gaps detected |
| Accuracy check — phase naming | Documentation claim (line 75): "in code (src/scheduler/unifiedSchedule.js), these sub-phases are called Phase 0 / Phase 1 / Phase 2 / Phase 3+4 / Phase 5" vs. actual code (unifiedScheduleV2.js line 580–590): phases are called 'V2: Immovable', 'V2: Constrained', 'V2: Unconstrained', 'V2: Retry' (NOT Phase 0/1/2/3/4/5) | INACCURATE: claimed phase names do not match actual code terminology |
| Accuracy check — file path claim | Documentation references "src/scheduler/unifiedSchedule.js" (line 75) but actual file is "src/scheduler/unifiedScheduleV2.js" | INACCURATE: file name in implementation note is wrong (missing V2 suffix) |
| Accuracy check — facade claim | Documentation does not mention the scheduler slice / hexagon facade, but user prompt indicates refactoring into src/slices/scheduler/ | INCOMPLETE: no mention of hexagon slice refactoring in the documentation; doc treats scheduler as monolithic |
| Accuracy check — core algorithm claims | Line 289–304 (4c-1 slack computation): claim "Slack computation — measures urgency as available_capacity(...) - duration". Verified against ConstraintSolver.js (slices/scheduler/domain/logic/) — concept matches but exact slack formula varies by constraint type (window vs. deadline). Claim is high-level and accurate. | ACCURATE: high-level description matches implementation intent |
| Accuracy check — placement order claims | Line 307–313 (4c-2 sort order): claim "All constrained items sorted: 1. Lowest slack first. 2. Highest priority wins ties. 3. Longest duration wins ties. 4. Deterministic ID final tie-break." Verified against unifiedScheduleV2.js comparator (line ~1050–1100) — sort logic matches claim. | ACCURATE: placement order matches code |
| Accuracy check — overlap prevention claims | Line 136: "The occupancy grid simply skips occupied slots and leaves lower-priority tasks where they are. Prevention is correct: eviction would silently unschedule tasks." Verified in unifiedScheduleV2.js occupancy grid logic (day Occ object, slot blocking). Prevention-not-eviction design confirmed. | ACCURATE: overlap prevention mechanism matches doc claim |
| Accuracy check — split chunks | Line 266–284 (split chunks section): claim "Chunk 1 id: masterUUID-<ordinal>; Chunk 2+ id: masterUUID-<ordinal>-<splitOrdinal>". Verified against reconcileOccurrences.js + unifiedScheduleV2.js split ID generation. Claim matches implementation. | ACCURATE: split chunk ID scheme matches code |
| Accuracy check — weather constraints | Line 189–234 (weather constraints section): claim "weatherOk(item, dateKey, startMin) is called inside findEarliestSlot for every candidate slot". Verified in unifiedScheduleV2.js — `weatherOk` is called within slot-search loop. Thresholds (dry_only 20%, clear 25%) match code constants. | ACCURATE: weather integration matches doc claim |
| Accuracy check — frontend nudge | Line 243–255 (frontend nudge section): claim "POST /schedule/nudge fired by frontend when task's scheduled end time passes". Verified in schedule.routes.js:POST /nudge and frontend code. Visibility-gating and 15-minute timeout mentioned. Claim accurate. | ACCURATE: frontend nudge mechanism matches doc |
| Code example extraction | awk fence extraction: js/bash code blocks in doc (examples in §9, §Weather, etc.). Example at line 197–206 shows weather query syntax. Parsed for correctness. | Code blocks are pseudocode / conceptual examples; not expected to compile verbatim |
| Link verification — internal | Links: #"Core principle", #"Severity hierarchy", #"Placement phases", etc. — all anchor to ## headings in the doc. Sample: line 95 "#### 4b. Recurring instances" can be linked as #recurring-instances-constrained-first. | Anchor links are same-document; valid |
| Link verification — external | No external links detected in SCHEDULER.md (appropriate for an internal spec). References to related docs (TASK-PROPERTIES.md, TASK-STATE-MATRIX.md) are mentioned in prose but not hyperlinked. | No broken external links |
| Frontmatter freshness | last_updated: 2026-05-25; today is 2026-06-12 → 18-day-old (stale for active doc) | STALE: last_updated field should be refreshed to 2026-06-12 if this is the authoritative spec post-refactor |
| Doc version alignment | type: design, status: active. The scheduler refactor (slices/scheduler/) was completed; doc should reflect hexagon slice reality. Doc treats scheduler as pre-refactor monolithic. | PARTIAL: doc version misaligned with implementation state (pre-refactor tone; no mention of slice architecture) |
| Orphan-doc check | SCHEDULER.md is referenced in juggler/CLAUDE.md (§Scheduler Key Files) and cross-referenced in TASK-PROPERTIES.md. Not orphaned. | COVERED: doc is discoverable and referenced |
| Required sections per BASE-ARCH-DOC-STANDARD | §1–6: Context (setup, what problem does this solve) · Containers (C4 L2 or component breakdown) · Key components · Data flow · Service boundaries · Tech stack · ADR links · Rationale/constraints. Verify presence: Context (line 22–46, Core principle + Severity hierarchy) · Containers (no explicit C4 diagram, but component descriptions: slots, phases, occupancy grid) · Data flow (implicit in 4a–4e; not explicit C4-style diagram) · Tech stack (SQL, UTC, occupancy grid = tech explicit; no formal tech-stack section) · ADR (line 383–395, "What changed from prior version" serves as post-hoc ADR; no link to a separate ADR.md) | PARTIAL: Context clear; Containers/Data-flow described but not in C4-formal style; no linked ADR per §6 of ARCH-DOC-STANDARD. Add reference to the hexagon-slice ADR if one exists. |
| Severity mapping — accuracy findings | BLOCK findings: (1) Phase naming incorrect (claimed Phase 0/1/2 vs. actual V2: Immovable/Constrained/...); (2) File path incorrect (claimed unifiedSchedule.js vs. actual unifiedScheduleV2.js). Per rubric §2 HIGH "Accuracy: API endpoints and parameters match implementation" → similar for algorithm details and file paths in a design doc. | HIGH defects (BLOCK) found on lines 75 + 75 |
| Severity mapping — completeness findings | WARN findings: (1) Frontmatter stale (last_updated 2026-05-25, today 2026-06-12); (2) Doc does not acknowledge hexagon-slice refactoring (architectural state drift); (3) Missing explicit C4/data-flow diagrams per BASE-ARCH-DOC-STANDARD §2. | MED defects (WARN) found |
| Output written | Write .planning/docs-sync/DOCS-REVIEW-scheduler-design.md | Done |

---

## Proof Checklist

- [x] Required inputs present (BASE-DOCUMENTATION-RUBRIC.md + BASE-ARCH-DOC-STANDARD.md loaded, SCHEDULER.md present, non-empty)
- [x] Mode confirmed (chore) — Accuracy + Structure verification on architecture-overview type doc; other dimensions INFO per mode scope
- [x] Doc file resolved (juggler-backend/docs/architecture/SCHEDULER.md) and is non-empty (756 lines, comprehensive design spec)
- [x] --re-review leg: prior findings (if any) would be in .planning/kermit/reviews/DOCS-REVIEW.md; this is the first full prairie verification of SCHEDULER.md (no prior review found)
- [x] Doc in scope: required sections per rubric §0 (architecture-overview type): Context · Containers · Key components · Data flow · Tech stack · ADR links — all present (some formal gaps noted)
- [x] Accuracy verified SEMANTICALLY: phase naming claim verified against actual code (unifiedScheduleV2.js) — terminology mismatch found (BLOCK); file path claim verified (unifiedScheduleV2.js not unifiedSchedule.js — BLOCK); core algorithm claims (slack, sort order, overlap prevention) verified against code — accurate; weather/nudge integration verified — accurate
- [x] Code examples: pseudocode/conceptual (not expected to compile verbatim); parsing check: no syntax errors in illustrative examples
- [x] Internal links checked: all heading anchors (#core-principle, #severity-hierarchy, etc.) resolve to ## targets in the same file; no dead fragments
- [x] Orphan-doc check: SCHEDULER.md is referenced in juggler/CLAUDE.md and related docs; not orphaned
- [x] External links: none present (appropriate for internal spec)
- [x] Frontmatter freshness checked: last_updated=2026-05-25 (18 days old, stale for active status); should be refreshed to 2026-06-12 if spec is being used post-refactor
- [x] Version/state alignment: doc predates hexagon-slice refactoring in tone; no acknowledgment of slices/scheduler/ architecture; doc feels pre-refactor
- [x] Severity mapping applied: HIGH defects (BLOCK) on phase naming and file path (line 75); MED defects (WARN) on stale frontmatter and missing hexagon-slice acknowledgment; other dimension findings INFO
- [x] Flag-and-refer: none required (no out-of-column issues for abby or other agents)
- [x] Rubric Coverage Map emitted: all 8 dimensions reviewed (see below)
- [x] Findings table present and emitted
- [x] Status line set: ISSUES (BLOCK findings present)
- [x] Scooter consultation: not needed (SCHEDULER.md is documentation of existing implementation, not a new requirement/standard/decision authored this leg)
- [x] Knowledge changes to INBOX: not applicable (no governing doc authored this leg; architectural refactoring already completed)

---

## Findings

| # | Severity | File:Line | Dimension | Description | Required Fix / Refer |
|---|----------|-----------|-----------|-------------|----------------------|
| 1 | BLOCK | SCHEDULER.md:75 | Accuracy | Phase naming claim incorrect: documentation states "in code (src/scheduler/unifiedSchedule.js), these sub-phases are called Phase 0 / Phase 1 / Phase 2 / Phase 3+4 / Phase 5" but actual code (unifiedScheduleV2.js lines 580–590, emitStepRecord) uses phase naming 'V2: Immovable', 'V2: Constrained', 'V2: Unconstrained', 'V2: Retry'. The phase numbers do not appear in the code; phase terminology in the doc contradicts the code. | REFER→abby: Correct phase naming on line 75. Options: (A) Change doc to match code phase names (V2: Immovable/Constrained/Unconstrained/Retry); (B) Explain the mapping from doc numbering (4a–4e) to actual phase strings in the code. Recommend (B) with a clear reference table. This is a rubric §2 HIGH defect. |
| 2 | BLOCK | SCHEDULER.md:75 | Accuracy | File path claim incorrect: documentation references "src/scheduler/unifiedSchedule.js" but actual file is "src/scheduler/unifiedScheduleV2.js" (missing V2 suffix). Callers (runSchedule.js, facade.js) explicitly import unifiedScheduleV2.js, not unifiedSchedule.js. | REFER→abby: Correct file path on line 75 from "unifiedSchedule.js" to "unifiedScheduleV2.js". This is a rubric §2 HIGH defect ("API endpoints and parameters match implementation" analog for algorithms/file paths). |
| 3 | WARN | SCHEDULER.md:1–15 | Accuracy | Frontmatter last_updated=2026-05-25 is stale (18 days old as of 2026-06-12). If this doc reflects the current hexagon-slice scheduler post-refactoring, the version should be refreshed to 2026-06-12 and version field updated to the latest commit SHA (e.g., from `git rev-parse --short HEAD` of juggler-backend). | REFER→abby: Refresh last_updated to 2026-06-12 and confirm version tag aligns with juggler-backend HEAD or the refactor's landing commit. This is a rubric §3 MED defect ("Version alignment — doc version matches code version"). |
| 4 | WARN | SCHEDULER.md:entire | Completeness | Documentation does not acknowledge the hexagon-slice refactoring (src/slices/scheduler/ architecture) despite the user prompt indicating it is the current implementation. The doc discusses scheduler as a monolithic set of functions (runSchedule.js, unifiedScheduleV2.js) but does not mention: (A) that the scheduler has been factored into domain/adapters/application/facade layers per hexagon architecture; (B) the scheduler slice facade (src/slices/scheduler/facade.js) as the new entry point; (C) the relationship between legacy src/scheduler/* files and the new slice structure. Per BASE-ARCH-DOC-STANDARD §4 (Containers), a refactored architecture doc should name the slice and explain its layer structure. | REFER→abby: Add a section (§1.1 or new "Architecture Refactoring" subsection after line 20–21) explaining: (1) The scheduler has been refactored into a hexagon slice (src/slices/scheduler/); (2) The slice exposes runScheduleAndPersist, getSchedulePlacements, unifiedScheduleV2 via facade.js; (3) Domain core (ConstraintSolver, ConflictResolver, ScoreEngine) + adapters (TaskProvider, CalendarProvider, ScheduleRepository, etc.) live in the slice; (4) The legacy src/scheduler/* files are still imported by the facade but may be thinned in future waves (H7). This is a rubric §0 completeness gap (architecture-overview should acknowledge major structural changes). |
| 5 | INFO | SCHEDULER.md:throughout | Standards | No explicit C4 L2 (Containers) or C4 L3 (Components) diagram present. The doc describes phases and algorithms in prose and tables but does not visualize the system context (what services call the scheduler? where does it fit in the broader architecture?). Per BASE-ARCH-DOC-STANDARD §2, architecture docs should include at least a C4 L2 showing Scheduler ← Routes/MCP, ← Database, ← Calendar/Weather services. | REFER→abby (if updating the doc): Consider adding a C4 L2 diagram (Mermaid or ASCII) showing: (1) HTTP routes → scheduler facade; (2) MCP tools → scheduler facade; (3) scheduler → task repository, calendar repository, weather repository; (4) scheduler → database (persist via RunScheduleCommand); (5) task/calendar/weather services as external ports. This is a rubric §3 LOW defect ("Searchable structure" – visual diagram aids discoverability); treat as INFO given the prose description is comprehensive. |
| 6 | INFO | SCHEDULER.md:throughout | Code Documentation | No JSDoc or per-function documentation linked for the scheduler's public entry points (runScheduleAndPersist, getSchedulePlacements, unifiedScheduleV2). The doc references them (line 75) but does not provide links to their JSDoc or source-code definitions. Developers reading this doc to call the scheduler would benefit from links to the public-API definitions. | REFER→abby (if updating): Add prose-level API reference or link section (e.g., "## Public API Reference") that lists the three public operations with their signatures and brief descriptions (one-liner). This is a rubric §6 LOW defect (not blocking; the doc describes the internals thoroughly but doesn't formally document the public surface). Treat as INFO. |

---

## Coverage Map

| Dimension | Status | Evidence | Notes |
|-----------|--------|----------|-------|
| Completeness | PARTIAL | All major algorithmic sections present (core principle, severity hierarchy, phases 4a–4e, deep-dive 4c, edge cases, verification/tests, file changes). Missing: hexagon-slice acknowledgment (no mention of src/slices/scheduler/ refactoring); public API reference (runScheduleAndPersist, getSchedulePlacements, unifiedScheduleV2 are referenced but not formally documented with signatures/links). | chore mode scopes to Accuracy + Structure; Completeness gap on architectural context (hexagon slice) is a WARN per rubric §1 HIGH "Setup instructions cover all environments" analog (architectural refactoring should be explained). |
| Accuracy | ISSUES | Phase naming claim incorrect (Phase 0/1/2 vs. V2: Immovable/Constrained/Unconstrained — line 75 BLOCK). File path incorrect (unifiedSchedule.js vs. unifiedScheduleV2.js — line 75 BLOCK). Core algorithm claims (slack, sort order, overlap prevention) verified accurate. Weather/nudge integration accurate. Split-chunk ID scheme accurate. Frontmatter stale (last_updated 18 days old — WARN). | PRIMARY focus for chore mode — 2 BLOCK defects on code terminology/paths; 1 WARN on staleness. Remaining accuracy claims spot-checked and verified. |
| Structure | covered | All required sections per rubric §3 present: Core principle (§ heading) · Severity hierarchy (table + numbered list) · Placement phases (§4 with subsections 4a–4e) · Deep-dive 4c (comprehensive explanation) · Edge cases (§ with sub-cases) · Verification (§ with test principles) · File changes (table). Heading levels consistent (## for major sections, ### for subsections, #### for phases). Markdown syntax valid. | chore mode verifies Structure; all checks pass. No broken table structures or malformed markdown. |
| Standards | INFO | Markdown lint: doc-lint would return PASS verdict (frontmatter present, no broken fences, valid links as far as they go). RFC-2119 keywords: not applicable (architecture spec, not a requirements spec). Code-block fencing: pseudocode examples have language tags (none present, as they are conceptual). File naming convention: "SCHEDULER.md" follows project pattern. | chore mode relegates standards dimension to INFO; no violations detected. |
| Audience Match | INFO | Technical audience (developers, architects, test engineers building/maintaining the scheduler). Progressive disclosure: starts with core principle + severity hierarchy (high-level), proceeds to phases 4a–4e (implementation detail), then deep-dive 4c (expert level). Appropriate for a design spec. No jargon unexplained (all terms defined: stack, slack, chain, anchor, etc.). | chore mode scopes to primary dimensions; no audience mismatch. Doc is well-structured for progressive learning. |
| Code Documentation | PARTIAL | SCHEDULER.md is an architecture spec, not inline code documentation. JSDoc/inline comments (rubric §6 criterion) are out of scope for this document. However, the doc serves as high-level documentation of the scheduler domain; missing public-API reference section (runScheduleAndPersist, getSchedulePlacements, unifiedScheduleV2 are referenced in passing but not formally documented with links or signatures). | chore mode scopes to primary dimensions; Code Documentation dimension is INFO. Public-API reference gap is a LOW defect (nice-to-have, not blocking). |
| Operational Docs | INFO | Runbook/monitoring/alerts out of scope for a design spec. The doc describes algorithm behavior (past-due handling, overlap prevention, weather constraints) which informs operational behavior, but does not prescribe monitoring dashboards, alert thresholds, or incident response procedures. Those would live in a separate RUNBOOK.md. No operational gaps in the scope of a design doc. | chore mode scopes to primary dimensions; not expected in a design spec. Operational Docs dimension is INFO. |
| Legal / Compliance | INFO | No PII/licensing/compliance issues in a technical design spec. No open-source attribution or license headers required in the doc itself (code files would have those). | chore mode scopes to primary dimensions; not expected in a design spec. Legal/Compliance dimension is INFO. |

---

## Sign-off

**Reviewed by:** Prairie (documentation verifier)  
**Date:** 2026-06-12 (ISO)  
**Verdict:** ISSUES — juggler-backend/docs/architecture/SCHEDULER.md contains 2 BLOCK-severity accuracy defects (phase naming and file path on line 75) and 2 WARN-severity issues (stale frontmatter, missing hexagon-slice architectural context). Fixes required before publish. The core algorithm descriptions and verification test catalog are accurate and comprehensive; the defects are in documentation of implementation details that have drifted post-refactoring.

**Required actions (refer to abby):**
1. Line 75: Correct phase terminology (Phase 0/1/2 vs. V2: Immovable/Constrained/Unconstrained) or add mapping table
2. Line 75: Correct file path (unifiedSchedule.js → unifiedScheduleV2.js)
3. Frontmatter: Refresh last_updated to 2026-06-12 and version tag to current commit
4. New section (after line 20): Add hexagon-slice architectural context (domain/adapters/application/facade layers, facade.js as entry point)

After fixes, return for re-review (prairie --mode chore --re-review).
