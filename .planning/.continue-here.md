---
context: default
status: wip
last_updated: 2026-05-19T19:18:57Z
---

# Juggler Bug Fixes + UI Standards — 2026-05-19 (sessions 1+2)

Freeform session. NOT a formal GSD phase.

---

## What was done

### 1. Scheduler nudge bug — FIXED
`juggler-frontend/src/hooks/useTaskState.js` ~line 404: `status !== 'active'` → `isTerminalStatus(t.status)`.
Pending tasks now arm the nudge timer. Dentist task (stuck at 10:45am) was root cause.
`juggler-backend/docs/SCHEDULER.md` updated: §8 Rule 1 + §9 Triggers.

### 2. Data heal script — CREATED
`juggler-backend/scripts/heal-stale-pending-tasks.js`
Run: `! node juggler-backend/scripts/heal-stale-pending-tasks.js --dry-run` first.

### 3. DailyView grid contrast — FIXED
`juggler-frontend/src/theme/colors.js`: added `gridLine`/`gridLineSub` tokens.
`juggler-frontend/src/components/views/DailyView.jsx` lines ~1114, ~1278: use new tokens.

### 4. AI command tests +20 — HARDENED
`juggler-backend/tests/api/ai-command.test.js` + `tests/aiRateLimiter.test.js` — 13 → 33 tests.

### 5. AI-COMMANDS.md — CREATED
`juggler-backend/docs/AI-COMMANDS.md` — design doc, Mermaid diagram, error table.

### 6. CalendarGrid grid lines — FIXED (SESSION 2)
Root cause: `theme.border` (#E8E0D0 light) on `theme.bg` (#F5F0E8) = ~1.15:1 contrast. Invisible.
Fix: `juggler-frontend/src/components/schedule/CalendarGrid.jsx`
- Hour lines: `theme.border` → `theme.gridLine`
- Added 30-min sub-lines with `theme.gridLineSub` (dashed, only when hourHeight >= 30)
Affects ThreeDayView + WeekView (both use CalendarGrid).

### 7. SCurveTimeline clock card layout — FIXED (SESSION 2)
Root cause: cards wrapped ScheduleCard in extra layers (outer border + bg div + 4px accent + paddingLeft:8/Right:6 = ~32px stolen). StatusToggle needs ~147px min → overflow → text/buttons overlap.
Fix: `juggler-frontend/src/components/schedule/SCurveTimeline.jsx`
- Removed all wrapper layers
- ScheduleCard renders in bare positioning div
- onExpand now passed directly to ScheduleCard

---

## Outstanding

### PRIORITY 1 — Big Bird UI audit + tests
Run `/big-brid-ux` in fresh session. Must cover:
- ThreeDayView + WeekView: grid lines visible, sub-lines appear, light + dark
- SCurveView (clock): no control overlap, light + dark, 1280px + 375px
- Write tests: `juggler-frontend/src/components/views/__tests__/calendarGrid.test.jsx`, `scurveTimeline.test.jsx`

### PRIORITY 2 — Visual QA DailyView grid colors
Start dev server, check both themes manually.

### PRIORITY 3 — Run heal script
`! node juggler-backend/scripts/heal-stale-pending-tasks.js --dry-run`

### PRIORITY 4 — Pre-commit checklist
/simplify → tests pass → docs → Oscar → git commit --no-verify

---

## All changed files (juggler submodule — NOTHING COMMITTED)

```
juggler-frontend/src/hooks/useTaskState.js
juggler-frontend/src/theme/colors.js
juggler-frontend/src/components/views/DailyView.jsx
juggler-frontend/src/components/schedule/CalendarGrid.jsx   SESSION 2
juggler-frontend/src/components/schedule/SCurveTimeline.jsx SESSION 2
juggler-backend/docs/SCHEDULER.md
juggler-backend/scripts/heal-stale-pending-tasks.js        NEW
juggler-backend/tests/api/ai-command.test.js
juggler-backend/tests/aiRateLimiter.test.js
juggler-backend/docs/AI-COMMANDS.md                        NEW
```

<next_action>
/gsd-resume-work → /big-brid-ux for ThreeDayView + WeekView grid lines + SCurveView card layout audit + tests
</next_action>
