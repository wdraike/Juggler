.superpowers/
node_modules/
.DS_Store
*.log
dist/
build/
.env
.env.local
.env.production
.env.test
.env.tmp
coverage/
.cache/
test-results/
playwright-report/
*.db
scripts/db-backup/
.playwright-mcp/
# auth-client source is vendored + tracked (needed by juggler-backend Dockerfile COPY,
# which runs in a clean CI checkout); only its installed deps stay ignored.
auth-client/node_modules/
.worktrees/
.claude/
.muppets/
juggler-backend/purge-orphan-events-*.csv

# MemPalace per-project files (issue #185)
mempalace.yaml
entities.json

# --- transient muppet/kermit per-leg state + tooling junk (cross-session churn guard) ---
.planning/kermit/
